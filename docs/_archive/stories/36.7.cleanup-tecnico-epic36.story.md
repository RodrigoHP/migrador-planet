# Story 36.7 — Cleanup tecnico Epic 36

- **ID:** 36.7
- **Title:** Cleanup tecnico Epic 36
- **Type:** fix
- **Status:** Done
- **Priority:** P3
- **Epic:** 36
- **Complexity:** S

## Story

As a developer, I want to clean up 3 technical debts introduced during Epic 36 (input element leak, missing version validation, naming convention violation) so that the codebase stays maintainable.

## Acceptance Criteria

- [x] `onOpen()` in TopToolbar.vue removes the ephemeral `<input>` element after use (no DOM leak)
- [x] `loadFromSavedProject()` warns via `console.warn` when an unknown `version` is encountered, but still attempts the load
- [x] `_parseDataFile` is extracted as a module-level function (not exposed as a Pinia action)
- [x] Tests cover unknown version warning, known version no-warning, and _parseDataFile not exposed

## Tasks

- [x] Fix input element leak in onOpen() — add input.remove() in finally block
- [x] Add KNOWN_PROJECT_VERSIONS set and version check in loadFromSavedProject
- [x] Extract _parseDataFile as module-level function parseDataFile
- [x] Add 4 new tests in sessionStore.spec.ts

## Scope

**IN:** TopToolbar.vue onOpen, session.ts loadFromSavedProject + parseDataFile extraction
**OUT:** Backend changes, new features, UI changes

## File List

- `frontend/src/organisms/TopToolbar.vue`
- `frontend/src/stores/session.ts`
- `frontend/src/stores/__tests__/sessionStore.spec.ts`

## Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-04-08 | @dev | Story created and implemented: 3 fixes + 4 tests |
