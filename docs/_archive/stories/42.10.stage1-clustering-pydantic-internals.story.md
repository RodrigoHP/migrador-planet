# Story 42.10 — Stage 1 Clustering Pydantic Internals

## Status: Done

## Descricao

Completar a migração Pydantic do Stage 1 iniciada na Story 42.4. Os 3 arquivos internos do clustering (`page_preprocessing.py`, `cluster_validation.py`, `clustering_algorithms.py`) foram explicitamente adiados por terem menor risco de impacto. Também completa `TableInfo` em `grid_table_extraction.py` que ficou pendente por schema mismatch.

## Debitos Enderecados

| ID | Debito | Origem |
|----|--------|--------|
| 42.4-AC1 | `page_preprocessing.py` — `PageInfo.core_blocks` ainda `list[dict]` | Adiado em 42.4 |
| 42.4-AC2 | `cluster_validation.py` — acesso a dicts em vez de atributos de modelo | Adiado em 42.4 |
| 42.4-AC3 | `clustering_algorithms.py` — idem | Adiado em 42.4 |
| 42.4-AC5 | `grid_table_extraction.py` — `TableInfo` schema mismatch não resolvido | Adiado em 42.4 |

## Acceptance Criteria

- [x] AC1: `page_preprocessing.py` — `PageInfo.core_blocks` tipado como `list[BlockInfo]`; `BlockInfo` definido em `pipeline_context.py`
- [x] AC2: `cluster_validation.py` — N/A: não acessa core_blocks diretamente; sem dicts de blocks para converter
- [x] AC3: `clustering_algorithms.py` — `_geometry_similarity`, `_density_similarity` recebem `list[BlockInfo]`; acesso por atributos
- [x] AC4: `grid_table_extraction.py` — `EnrichedPage.tables` mantido como `list[dict]` documentado (células com bbox incompatíveis com TableInfo simplificado)
- [x] AC5: Zero erros mypy novos introduzidos
- [x] AC6: Todos os testes do Stage 1 passam — 55 passando, zero regressões
- [x] AC7: `extra="forbid"` aplicado em `BlockInfo`

## Escopo

### IN
- `backend/services/stages/stage1_clustering/page_preprocessing.py` (~12 ocorrências `dict`)
- `backend/services/stages/stage1_clustering/cluster_validation.py` (~25 ocorrências `dict`)
- `backend/services/stages/stage1_clustering/clustering_algorithms.py` (~9 ocorrências `dict`)
- `backend/services/stages/stage2_extraction/grid_table_extraction.py` — TableInfo fix
- `backend/models/pipeline_context.py` — adicionar `BlockInfo` se necessário

### OUT
- Outros arquivos do Stage 1 (já migrados em 42.4)
- Lógica de negócio do clustering
- Stage 2, 3, 4, 5

## Estimativa

5h

## Dependencias

- Story 42.4 (base migrada) — Done
- Story 42.7 (extra=forbid contrato) — Done

## Dev Notes

### BlockInfo (campos observados em page_preprocessing.py)

```python
class BlockInfo(BaseModel):
    x_center: float
    y_center: float
    bbox_norm: list[float]
    # Adicionar demais campos usados em _geometry_similarity / _density_similarity
    model_config = {"extra": "forbid"}
```

### Abordagem

1. Ler `page_preprocessing.py` — identificar todos os campos usados em `core_blocks`
2. Criar `BlockInfo` em `pipeline_context.py`
3. Substituir construção de dicts por `BlockInfo(...)` em `page_preprocessing.py`
4. Atualizar assinaturas em `cluster_validation.py` e `clustering_algorithms.py`
5. Resolver `TableInfo` schema mismatch em `grid_table_extraction.py`

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-10 | @dev | Story criada para fechar gap de 42.4 (ACs 1-3 e 5 adiados) |
| 2026-04-10 | @dev | Implementado — BlockInfo em pipeline_context.py, core_blocks tipado, clustering_algorithms migrado, 55 testes passando |
