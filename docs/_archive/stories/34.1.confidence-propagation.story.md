# Story 34.1 — Propagar score de confianca real ao frontend

- **ID:** 34.1
- **Title:** Propagar score de confianca real ao frontend
- **Type:** feature
- **Status:** Ready
- **Priority:** P1
- **Epic:** 34
- **Complexity:** M

## Story

As a template operator, I want to see real confidence scores from the pipeline instead of hardcoded 'medium', so that I can trust the mapping quality indicators.

## Acceptance Criteria

- [x] Extend `FieldMappingEntry` type em `pipeline.types.ts` com campo `confidence: number | string`
- [x] `loadPipelineFields()` usa `entry.confidence` real do pipeline (nao hardcoded 'medium')
- [x] `FieldMappingTable` exibe ConfidenceBadge com score real (0-100 ou low/medium/high derivado)
- [x] FieldNavigator reflete confianca correta nos badges de status
- [x] Score numerico do stage4 (`confidence_score`) mapeado para categorias: <40% low, 40-75% medium, >75% high

## Tasks

- [x] Add `confidence` field to `FieldMappingEntry` in `pipeline.types.ts`
- [x] Add `confidence` field to `FieldNavItem` in `field-navigator.types.ts`
- [x] Update `loadPipelineFields()` in `mapping.ts` to propagate real confidence
- [x] Add `mapConfidenceLevel()` helper function for numeric-to-category mapping
- [x] Update tests

## Scope

**IN:** FieldMappingEntry type extension, confidence propagation in mapping store, category mapping
**OUT:** Backend changes to stage4, UI redesign of FieldNavigator

## Dev Notes

- The backend already sends `confidence_score` in field_mappings entries
- Need to map numeric scores to low/medium/high categories

## Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-04-08 | @dev | Implemented and tested |
