---
epic: 6
story: 1
title: "Pinia Stores Foundation — 7 Novos Stores + 4 Expandidos para Estado do Editor"
status: Done
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [static_analysis]
depends_on: [5.11]
---

# Story 6.1: Pinia Stores Foundation — 7 Novos Stores + 4 Expandidos para Estado do Editor

## Status
Done

## Story
**As a** sistema frontend,
**I want** 7 novos Pinia stores (editor, template, inspector, coverage, confidence, testData, multiDoc) e 4 stores existentes expandidos (session, mapping, layout, generation), todos populados a partir da resposta da API do pipeline,
**so that** o editor tenha estado reativo para renderizar todos os componentes.

## Acceptance Criteria
1. `editorStore`: gerencia aba ativa (canvas/pdf/code/sync), nível de zoom, estado de seleção, aba do sidebar, toggles (coverage mode, diff mode, snap)
2. `templateStore`: mantém a árvore de estrutura do documento (source of truth) — Document > Header > Flow > Footer > elementos, com nós contendo `{ id, type, name, binding, children, properties, visibility }`
3. `inspectorStore`: rastreia nó selecionado, nível atual (page/section/component/element), propriedades para exibição
4. `coverageStore`: armazena dados de cobertura por Layout Type — `{ fields: {mapped, total}, tables: {mapped, total}, images: {mapped, total}, charts: {mapped, total}, percentage }`
5. `confidenceStore`: armazena 5 fatores por Layout Type — `{ layout_stability, anchor_detection, grid_quality, field_variability, vision_agreement, overall }`
6. `testDataStore`: gerencia lista de datasets, dataset ativo, resultados de validação (para Epic 8)
7. `multiDocStore`: gerencia lista de PDFs (base/variação), matriz de variação, detecções (para Epic 9)
8. Stores existentes expandidos: `sessionStore` (job_id, template_name), `mappingStore` (field_mappings do pipeline), `layoutStore` (layout_types, active_layout), `generationStore` (template_draft HTML/CSS)
9. Action `loadFromPipelineResult(result: PipelineResult)` no sessionStore que despacha dados para todos os stores
10. Todos os stores possuem interfaces TypeScript
11. Testes cobrem: inicialização dos stores, loadFromPipelineResult dispatching, atualizações reativas

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [x] Task 1 — Definir interfaces TypeScript para todos os stores (AC: 10)
  - [x] Criar `frontend/src/types/editor.types.ts` com interfaces: EditorState, CenterTab, LeftTab, ToggleStates
  - [x] Criar `frontend/src/types/template.types.ts` com interfaces: DocumentTree, TreeNode, NodeType, NodeProperties
  - [x] Criar `frontend/src/types/inspector.types.ts` com interfaces: InspectorState, InspectorLevel
  - [x] Criar `frontend/src/types/coverage.types.ts` com interfaces: CoverageData, CoverageBreakdown, CoverageByLayout
  - [x] Criar `frontend/src/types/confidence.types.ts` com interfaces: ConfidenceFactors, ConfidenceByLayout
  - [x] Criar `frontend/src/types/pipeline.types.ts` com interface: PipelineResult (shape completa do JSON de resposta)
  - [x] Criar `frontend/src/types/test-data.types.ts` com interfaces: Dataset, ValidationResult
  - [x] Criar `frontend/src/types/multi-doc.types.ts` com interfaces: PdfDocument, VariationMatrix

- [x] Task 2 — Criar editorStore (AC: 1)
  - [x] Criar `frontend/src/stores/editorStore.ts` usando composition API (setup store)
  - [x] Estado: `activeCenterTab` (canvas|pdf|code|sync), `activeLeftTab` (structure|fields), `zoomLevel` (number, default 100), `selectedElementId` (string|null), `activeSidebarTab` (string)
  - [x] Toggles: `coverageMode` (boolean), `diffMode` (boolean), `snapEnabled` (boolean), `autoFixEnabled` (boolean), `showGuides` (boolean)
  - [x] Estado PDF: `pdfZoom` (number, default 100)
  - [x] Actions: `setActiveCenterTab()`, `setActiveLeftTab()`, `setZoom()`, `selectElement()`, `toggleCoverage()`, `toggleDiff()`, `toggleSnap()`, `toggleAutoFix()`, `toggleGuides()`

- [x] Task 3 — Criar templateStore (AC: 2)
  - [x] Criar `frontend/src/stores/templateStore.ts` usando composition API
  - [x] Estado: `documentTree` (DocumentTree), `flatNodes` (Map<string, TreeNode> para lookup rápido)
  - [x] Getters: `getNodeById(id)`, `getNodesByType(type)`, `getRootNode()`, `getChildren(parentId)`
  - [x] Actions: `loadTree(tree: DocumentTree)`, `updateNodeProperties(id, props)`
  - [x] Garantir que templateStore é SOURCE OF TRUTH — template.json é apenas serialização

- [x] Task 4 — Criar inspectorStore (AC: 3)
  - [x] Criar `frontend/src/stores/inspectorStore.ts` usando composition API
  - [x] Estado: `selectedNode` (TreeNode|null), `level` (page|section|component|element), `properties` (Record<string, any>)
  - [x] Getters: `currentLevelLabel()`, `hasSelection()`
  - [x] Actions: `selectNode(node: TreeNode)`, `clearSelection()`, `setLevel(level)`
  - [x] Auto-detectar nível baseado no tipo do nó: document→page, section→section, table/chart/image/container→component, text/field→element

- [x] Task 5 — Criar coverageStore (AC: 4)
  - [x] Criar `frontend/src/stores/coverageStore.ts` usando composition API
  - [x] Estado: `coverageByLayout` (Map<string, CoverageData>)
  - [x] Getters: `getForLayout(layoutId)`, `activeLayoutCoverage` (baseado em layoutStore.activeLayoutId)
  - [x] Getter `thresholdLevel`: ≥95% → 'complete', 80-95% → 'review', <80% → 'incomplete'
  - [x] Actions: `loadCoverage(data)`, `updateForLayout(layoutId, coverage)`

- [x] Task 6 — Criar confidenceStore (AC: 5)
  - [x] Criar `frontend/src/stores/confidenceStore.ts` usando composition API
  - [x] Estado: `confidenceByLayout` (Map<string, ConfidenceFactors>)
  - [x] Getters: `getForLayout(layoutId)`, `activeLayoutConfidence`, `overallForActiveLayout`
  - [x] Getter `thresholdLevel`: ≥95% → 'approved', 80-95% → 'review', <80% → 'human_review'
  - [x] Actions: `loadConfidence(data)`, `updateForLayout(layoutId, confidence)`

- [x] Task 7 — Criar testDataStore e multiDocStore (AC: 6, 7)
  - [x] Criar `frontend/src/stores/testDataStore.ts` — estado: datasets[], activeDatasetId, validationResults[]
  - [x] Criar `frontend/src/stores/multiDocStore.ts` — estado: pdfList[], variationMatrix, detections[]
  - [x] Ambos com actions placeholder para Epics 8 e 9

- [x] Task 8 — Expandir stores existentes (AC: 8)
  - [x] Expandir `sessionStore`: adicionar `job_id`, `template_name`, `uploadedPdfs[]`, `analysisCompleted`
  - [x] Expandir `mappingStore`: adicionar `loadPipelineFields()` com interface `{ name, path, type, status, binding?, isOptional }`
  - [x] Expandir `layoutStore`: adicionar `layoutTypes[]`, `activeLayoutId`, `activeLayout` getter
  - [x] Expandir `generationStore`: adicionar `templateDraft: { html: string, css: string }`

- [x] Task 9 — Implementar loadFromPipelineResult (AC: 9)
  - [x] Adicionar action `loadFromPipelineResult(result: PipelineResult)` no sessionStore
  - [x] Dispatch para templateStore: `loadTree(result.document_structure)`
  - [x] Dispatch para mappingStore: carregar `result.field_mappings`
  - [x] Dispatch para confidenceStore: carregar `result.confidence_scores`
  - [x] Dispatch para coverageStore: carregar `result.coverage`
  - [x] Dispatch para layoutStore: carregar `result.layout_types`
  - [x] Dispatch para generationStore: carregar `result.template_draft`

- [x] Task 10 — Escrever testes (AC: 11)
  - [x] Criar `frontend/src/stores/__tests__/editorStore.spec.ts` — inicialização, toggle actions
  - [x] Criar `frontend/src/stores/__tests__/templateStore.spec.ts` — loadTree, getNodeById
  - [x] Criar `frontend/src/stores/__tests__/coverageStore.spec.ts` — loadCoverage, thresholds
  - [x] Criar `frontend/src/stores/__tests__/confidenceStore.spec.ts` — loadConfidence, thresholds
  - [x] Criar `frontend/src/stores/__tests__/sessionStore.spec.ts` — loadFromPipelineResult dispatching
  - [x] Executar `npm run test` e garantir que todos passam

## Dev Notes

### Shape do JSON do Pipeline
O pipeline (Epic 5) retorna um JSON com a seguinte estrutura:
```typescript
interface PipelineResult {
  document_structure: DocumentTree;       // árvore hierárquica
  field_mappings: FieldMapping[];         // mapeamentos campo→binding
  confidence_scores: Record<string, ConfidenceFactors>; // por layout type
  coverage: Record<string, CoverageData>;              // por layout type
  layout_types: LayoutType[];             // tipos de layout detectados
  template_draft: { html: string; css: string };        // HTML+CSS gerado
  ambiguous_fields: AmbiguousField[];     // campos ambíguos para revisão
  format_functions: FormatFunction[];     // funções de formatação detectadas
}
```

### Pinia 3.x com Composition API
Todos os stores devem usar o padrão setup store:
```typescript
export const useEditorStore = defineStore('editor', () => {
  const activeCenterTab = ref<CenterTab>('canvas')
  // ...
  return { activeCenterTab, /* ... */ }
})
```

### templateStore é SOURCE OF TRUTH
- A árvore de documento no templateStore é a fonte de verdade
- template.json é apenas serialização para save/export
- Nós da árvore: `{ id, type, name, binding, isOptional, children[], properties, visibility }`

### Thresholds de Cobertura e Confiança
- Cobertura: ≥95% complete ✅, 80-95% review recommended ⚠️, <80% incomplete 🔴
- Confiança: ≥95% approved ✅, 80-95% review recommended ⚠️, <80% human review 🔴
- Ambos são POR Layout Type — ao trocar layout, as métricas atualizam

### Diretório dos Stores
- Stores existentes: `frontend/src/stores/`
- Novos stores no mesmo diretório
- Types em `frontend/src/types/`

### FRs Referenciados
- FR7 (template preview), FR8 (field mapping), FR29 (confidence), FR33 (coverage), FR36 (editor structure), FR38 (document tree), FR39 (inspector), FR43 (PDF reference)

### Testing
- Vitest for unit tests
- Test files alongside source: `*.spec.ts` or `*.test.ts`

## File List
### Created
- `frontend/src/types/editor.types.ts`
- `frontend/src/types/template.types.ts`
- `frontend/src/types/inspector.types.ts`
- `frontend/src/types/coverage.types.ts`
- `frontend/src/types/confidence.types.ts`
- `frontend/src/types/pipeline.types.ts`
- `frontend/src/types/test-data.types.ts`
- `frontend/src/types/multi-doc.types.ts`
- `frontend/src/stores/editorStore.ts`
- `frontend/src/stores/templateStore.ts`
- `frontend/src/stores/inspectorStore.ts`
- `frontend/src/stores/coverageStore.ts`
- `frontend/src/stores/confidenceStore.ts`
- `frontend/src/stores/testDataStore.ts`
- `frontend/src/stores/multiDocStore.ts`
- `frontend/src/stores/__tests__/editorStore.spec.ts`
- `frontend/src/stores/__tests__/templateStore.spec.ts`
- `frontend/src/stores/__tests__/coverageStore.spec.ts`
- `frontend/src/stores/__tests__/confidenceStore.spec.ts`
- `frontend/src/stores/__tests__/sessionStore.spec.ts`

### Modified
- `frontend/src/stores/session.ts` — added job_id, template_name, uploadedPdfs, loadFromPipelineResult
- `frontend/src/stores/mapping.ts` — added loadPipelineFields action
- `frontend/src/stores/layout.ts` — added layoutTypes, activeLayoutId, activeLayout getter, loadLayoutTypes, setActiveLayout
- `frontend/src/stores/generation.ts` — added templateDraft, loadTemplateDraft

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
| 2026-03-16 | 1.1 | Ajustes PO: depends_on | River (@sm) |
| 2026-03-17 | 1.2 | Implementação completa: 7 novos stores, 4 expandidos, 8 type files, 5 test files — 85 testes passando | Dex (@dev) |

---

## QA Results

**Reviewer:** Quinn (Test Architect)
**Date:** 2026-03-17
**Gate:** PASS

### Review Checklist
- ✓ Code Quality: código limpo, sem code smells, TypeScript correto (0 erros tsc --noEmit)
- ✓ Test Coverage: 85 testes passando, cobrindo todos os 5 stores exigidos por AC11
- ✓ AC Compliance: todos os 11 ACs cobertos (ver gate file)
- ✓ Security: sem XSS, sem injection, sem credenciais hardcoded
- ~ Performance: computed() eficientes, sem memory leaks — CONCERNS menores (ver abaixo)

### Issues Found

**ISSUE-1 (low):** `coverageStore` e `confidenceStore` acessam `layoutStore.activeLayoutId` via type cast `(layoutStore as unknown as { activeLayoutId?: string })`. O valor existe em runtime pois `layout.ts` foi expandido nesta story, mas o type cast é frágil. Não bloqueia.

**ISSUE-2 (low):** `testDataStore` e `multiDocStore` não possuem testes próprios. Por design — AC11 não os lista e ambos contêm apenas placeholders para Epics 8 e 9. Não bloqueia.

### Gate Status

**PASS** — Implementação completa, todos os ACs cobertos, 85/85 testes passando, TypeScript limpo. Issues encontrados são de severidade baixa e não bloqueantes. Gate file: `docs/qa/gates/6.1-pinia-stores.yml`.

---

## Architect Review

**Reviewer:** Aria (System Architect)
**Date:** 2026-03-17
**Status:** APPROVED_WITH_NOTES

### Design Assessment

**Coupling:** Baixo. Stores se comunicam apenas via `loadFromPipelineResult` (orquestrador no sessionStore com dynamic imports) e via referência ao `layoutStore` para o `activeLayoutId` ativo. Não há imports circulares em tempo de módulo — os dynamic imports em `loadFromPipelineResult` resolvem isso corretamente.

**Cohesion:** Alta. Cada store tem responsabilidade única e bem delimitada: `editorStore` → estado de UI do editor; `templateStore` → árvore de documento; `inspectorStore` → seleção; `coverageStore`/`confidenceStore` → métricas por layout; `testDataStore`/`multiDocStore` → placeholders para épicos futuros. Os 4 stores expandidos preservam comportamento anterior e adicionam apenas o necessário.

**Patterns:** Composition API (setup stores) corretamente usada nos 7 novos stores. Os 4 stores existentes mantêm options API por retrocompatibilidade — decisão aceitável. TypeScript interfaces completas em arquivos dedicados (`frontend/src/types/`). PipelineResult tipado com todas as entidades do pipeline.

**Scalability:** Os stores escalam para Epics 7, 8 e 9:
- `templateStore` expõe `loadTree()` — suficiente para Epic 7 (editor visual)
- `testDataStore` tem interface preparada para Epic 8 (test datasets)
- `multiDocStore` tem interface preparada para Epic 9 (multi-PDF)
- `loadFromPipelineResult` centraliza o bootstrap — Epic 7 só precisa chamar um método

### Issues Found

**ARCH-NOTE-1:** O acesso a `layoutStore.activeLayoutId` via type cast em `coverageStore` e `confidenceStore` é tecnicamente correto (o campo existe no state em runtime), mas fragiliza o contrato de tipo. Recomenda-se converter `layoutStore` para composition API quando houver oportunidade no Epic 7, eliminando a necessidade do cast.

**ARCH-NOTE-2:** `loadFromPipelineResult` usa dynamic `await import()` para evitar ciclos de módulo. Isso introduz uma latência negligível e é aceitável para a camada de inicialização de sessão. Alternativa futura: criar um `pipelineBootstrap` service externo que importe todos os stores e os popule — mas não é necessário agora.

### Technical Debt Registered

- **TD-6.1-A (low):** Converter `layoutStore` para composition API para eliminar type cast em coverage/confidence stores. Target: Epic 7.
- **TD-6.1-B (low):** Adicionar testes para `testDataStore` e `multiDocStore` quando Epics 8 e 9 adicionarem lógica substantiva.

### Decision Log

1. `loadFromPipelineResult` fica no `sessionStore` (não num service separado) para manter o padrão do projeto de ações em stores — evita introduzir nova camada de abstração antes do Epic 7.
2. `templateStore.flatNodes` é `Map<string, TreeNode>` para O(1) lookup — decisão correta para árvores grandes.
3. Dynamic imports em `loadFromPipelineResult` preferidos a imports estáticos circulares.

### Status

APPROVED_WITH_NOTES. A implementação está arquiteturalmente sólida para os Epics 6, 7, 8 e 9. As notas ARCH-NOTE-1 e ARCH-NOTE-2 são melhorias futuras, não bloqueantes. Story pode avançar para Done.
