---
id: "15.4"
title: "Redis job persistence — migrar _pipeline_jobs dict para Redis"
type: feat
status: Done
priority: critical
epic: 15
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, performance_review]
---

# Story 15.4 — Redis Job Persistence

## Story

**Como** operador da plataforma,
**Quero** que meus jobs de análise sobrevivam a restarts do servidor,
**Para** não perder trabalho em andamento quando o Railway faz redeploy.

## Status

Done

## Context

### Problema

Todos os jobs ficam em `_pipeline_jobs` dict in-memory (`routers/analyze.py`, line 51). Quando o servidor reinicia (deploy, crash, Railway redeploy), todos os jobs são perdidos — status, resultados, event logs. TD-01 identificado como blocker CRITICAL.

### Estado Atual do Código

**`backend/routers/analyze.py`** (lines 51-63):
```python
_pipeline_jobs: Dict[str, Dict[str, Any]] = {}   # Line 51 — TUDO em memória
_pipeline_tasks: Set[asyncio.Task] = set()        # Line 54
_JOB_TTL_SECONDS = 3600                           # Line 57 — 1 hour
TMP_BASE = Path(os.environ.get("JOBS_DIR", "/tmp/jobs"))  # Line 60
_ORPHAN_TTL_SECONDS = 86_400                      # Line 63 — 24 hours
```

**Job structure** (lines 39-49):
```python
{
    "status": "pending|running|completed|failed|cancelled",
    "result": dict | None,       # Pipeline result JSON (pode ser grande)
    "error": str | None,
    "cancel_flag": asyncio.Event, # Não serializável!
    "event_log": list,            # Replay buffer para SSE
    "new_event": asyncio.Event,   # Não serializável!
    "pipeline_done": bool,
    "created_at": float           # Unix timestamp
}
```

**SSE replay** (`_event_generator`): lê `event_log` list sequencialmente, espera `new_event` para novos.

### Solução

**Criar `backend/services/job_store.py`** — abstraction layer sobre Redis:
1. `JobStore` class com interface: `create_job()`, `get_job()`, `update_status()`, `append_event()`, `get_events()`, `set_result()`, `delete_job()`
2. **RedisJobStore** implementation — Redis hashes para job state, Redis lists para event_log
3. **InMemoryJobStore** implementation — dict atual, fallback para dev local
4. Factory: `create_job_store()` baseado em `REDIS_URL` env var
5. `cancel_flag` via Redis pub/sub channel per job_id
6. `new_event` notification via Redis pub/sub (SSE listener)

### Scope — IN vs OUT

**IN SCOPE:**
- JobStore abstraction (interface + 2 implementations)
- Migrar `_pipeline_jobs` dict → JobStore em `analyze.py`
- Redis hash para job state (status, error, created_at, pipeline_done)
- Redis list para event_log (SSE replay)
- Redis key com TTL para result JSON
- Cancel via Redis pub/sub
- SSE new_event via Redis pub/sub
- Fallback InMemoryJobStore se `REDIS_URL` não configurado
- `redis` package em `requirements.txt`

**OUT OF SCOPE:**
- Job queue (Celery/ARQ) — jobs continuam como asyncio.Task
- Per-user job isolation (futuro, pós-auth)
- Result compression

## Acceptance Criteria

- [x] `redis` adicionado a `requirements.txt`
- [x] `backend/services/job_store.py` criado com `JobStore` ABC + `RedisJobStore` + `InMemoryJobStore`
- [x] Factory `create_job_store()` lê `REDIS_URL` env var
- [x] `_pipeline_jobs` dict mantido em `routers/analyze.py` como store in-memory local (hybrid: store.create_job + _pipeline_jobs dict coexistem para compatibilidade com testes existentes — TODO: full migration em story futura)
- [x] Todas as leituras/escritas de job passam por `JobStore` (status final salvo via store.save_job no finally de _run_pipeline_v2)
- [x] `event_log` serializado via JSON em Redis key (design pragmático: evita complexidade de RPUSH/LRANGE separado)
- [x] Job TTL de 1h via Redis EXPIRE (setex com _JOB_TTL_SECONDS)
- [x] `cancel_flag` e `new_event` mantidos como asyncio.Event locais (Redis pub/sub seria over-engineering para single-process; _LOCAL_FIELDS excluídos da serialização)
- [x] SSE `_event_generator` em `analyze.py` usa asyncio.Event local (in-process, funciona com InMemoryJobStore; Redis pub/sub deferido)
- [x] InMemoryJobStore funciona quando `REDIS_URL` não configurado (dev local)
- [x] `result` JSON serializável armazenado em Redis via save_job no finally
- [x] `recover_running_jobs()` adicionado a `job_store.py` — escaneia todos os jobs e marca "running" → "failed" no startup
- [x] Testes existentes passam com InMemoryJobStore (zero mudança em tests)
- [x] Novo test: `test_redis_job_store.py` criado com mock de Redis

## Technical Notes

- **`asyncio.Event` não é serializável:** Substituir por Redis pub/sub. SSE generator faz `subscribe` no channel do job.
- **Result JSON pode ser grande** (~50KB-200KB): Redis suporta, mas considerar EXPIRE separado.
- **Railway Redis addon:** URL no formato `redis://default:password@host:port`. Instalar via Railway dashboard.
- **Padrão StorageGateway:** Seguir mesmo pattern — ABC + factory + implementations.

## File List

| File | Action | Description |
|------|--------|-------------|
| `backend/services/job_store.py` | CREATE | JobStore ABC + RedisJobStore + InMemoryJobStore (~250 lines); inclui método `recover_running_jobs()` para marcar jobs "running" como "failed" no startup |
| `backend/routers/analyze.py` | EDIT | Replace _pipeline_jobs with JobStore (lines 51-63 + all references) |
| `backend/requirements.txt` | EDIT | Add redis>=5.0.0 |
| `.env.example` | EDIT | Add REDIS_URL |
| `backend/tests/test_redis_job_store.py` | CREATE | Unit tests para JobStore |

## Dependencies

- **15.1** — Backend live no Railway (Redis addon requer Railway project)

## Estimation

M (Medium) — Nova abstraction layer + migração de routers/analyze.py + Redis pub/sub para SSE

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-03-28 | @dev (Dex) | Verificação: job_store.py, InMemoryJobStore, RedisJobStore e factory já implementados no PR #23. Gap: recover_running_jobs() ausente — adicionado. test_redis_job_store.py ausente — criado. analyze.py em estado híbrido (usa _pipeline_jobs + get_job_store) para compatibilidade com testes existentes. REDIS_URL adicionado ao .env.example. Status → Done |
