---
epic: 6
story: 4
title: "FieldNavigator Auto-Populated — Lista de Campos com Status de Mapeamento"
status: Done
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [static_analysis, ui_behavior_check]
depends_on: [6.1, 6.2]
---

# Story 6.4: FieldNavigator Auto-Populated — Lista de Campos com Status de Mapeamento

## Status
Done

## Story
**As a** operador,
**I want** ver todos os campos XSD listados com seu status de mapeamento (mapeado/não mapeado/não confirmado), organizados por tipo,
**so that** eu possa rapidamente ver quais campos ainda precisam de atenção.

## Acceptance Criteria
1. Organism FieldNavigator renderiza campos de `mappingStore.fields` agrupados por tipo: 📋 Campos, 📊 Tabelas, 📈 Gráficos, 📐 Seções, 🖼️ Recursos
2. Cada campo exibe: ícone, nome, badge de status (🟩 mapeado, 🟥 não mapeado, 🟨 não confirmado)
3. Campos opcionais marcados com ⚠
4. Clicar em um campo seleciona o nó correspondente na StructureTree (atualiza inspectorStore)
5. Resumo de contagem no topo: "18 de 20 campos mapeados"
6. Status derivado de mappingStore: mapped = binding confirmado, unmapped = sem binding, unconfirmed = auto-mapeado mas não confirmado pelo operador
7. Campos ordenáveis por: nome, status, tipo
8. `npm run build` passa

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [x] Task 1 — Criar molecule FieldNavItem (AC: 2, 3)
  - [x] Criar `frontend/src/molecules/FieldNavItem.vue`
  - [x] Props: `field: FieldNavItem`, `isSelected: boolean`
  - [x] Renderizar ícone baseado em `field.type`: string→📋, array→📊, chart→📈, section→📐, image→🖼️
  - [x] Renderizar nome do campo: `field.name`
  - [x] Renderizar badge de status: mapped→🟩, unmapped→🟥, unconfirmed→🟨
  - [x] Renderizar badge ⚠ se `field.isOptional === true`
  - [x] Emitir evento @click para seleção

- [x] Task 2 — Criar organism FieldNavigator (AC: 1, 5)
  - [x] Criar `frontend/src/organisms/FieldNavigator.vue`
  - [x] Usar `mappingStore.fieldNavItems` como fonte de dados
  - [x] Computed: agrupar campos por tipo em seções com headers
  - [x] Header de cada grupo: ícone + nome do tipo + contagem (ex.: "📋 Campos (12)")
  - [x] Seções expansíveis/colapsáveis
  - [x] Resumo no topo: "N de M campos mapeados" com barra de progresso

- [x] Task 3 — Implementar contagem e resumo (AC: 5)
  - [x] Computed `mappedCount`: conta campos com status 'mapped'
  - [x] Computed `totalCount`: total de campos
  - [x] Exibir `${mappedCount} de ${totalCount} campos mapeados`
  - [x] ProgressBar (atom) mostrando percentual visualmente

- [x] Task 4 — Implementar seleção de campo (AC: 4)
  - [x] Ao clicar em um FieldNavItem, encontrar nó via `templateStore.getNodeById(field.nodeId)` ou busca por binding
  - [x] Chamar `inspectorStore.selectNode(node)` e `editorStore.selectElement(node.id)`
  - [x] Destacar visualmente o campo selecionado no FieldNavigator

- [x] Task 5 — Implementar ordenação (AC: 7)
  - [x] Botões de sort no topo: "Ordenar por: [Nome] [Status] [Tipo]"
  - [x] Sort por nome: alfabético
  - [x] Sort por status: unmapped primeiro, depois unconfirmed, depois mapped
  - [x] Sort por tipo: ordem dos grupos (TYPE_ORDER)

- [x] Task 6 — Integrar no LeftPanel (AC: 1)
  - [x] Inserir FieldNavigator no slot da aba "Campos" do LeftPanel (criado na 6.2)
  - [x] Scroll vertical quando lista excede altura do painel

- [x] Task 7 — Build e verificação (AC: 8)
  - [x] Executar `npm run build` e corrigir erros
  - [x] Criar teste unitário `FieldNavigator.spec.ts`: 19 testes cobrindo grupos, badges, seleção, contagem, sort, collapse, empty state

## Dev Notes

### Wireframe da Aba Campos
```
┌─────────────────────────┐
│ 18 de 20 campos mapeados│
│ ████████████████░░ 90%  │
│ Ordenar: [Nome ▼]       │
├─────────────────────────┤
│ 📋 Campos (12)      [▼]│
│  🟩 cliente              │
│  🟩 cpf                  │
│  🟨 endereco         ⚠  │
│  🟥 telefone             │
│  ...                     │
│ 📊 Tabelas (5)      [▼]│
│  🟩 transacoes           │
│  🟨 lancamentos          │
│  ...                     │
│ 📈 Gráficos (2)     [▼]│
│  🟩 grafico_mensal   ⚠  │
│  🟥 grafico_anual        │
│ 🖼️ Recursos (1)     [▼]│
│  🟩 logo                 │
└─────────────────────────┘
```

### Interface de Campo (de mappingStore)
```typescript
interface FieldMapping {
  name: string;
  path: string;          // XPath ou JSON path do campo
  type: 'string' | 'array' | 'chart' | 'section' | 'image';
  status: 'mapped' | 'unmapped' | 'unconfirmed';
  binding?: string;      // binding no template (ex.: "cliente")
  nodeId?: string;       // ID do nó correspondente no templateStore
  isOptional: boolean;
}
```

### Mapeamento type→ícone e grupo
```typescript
const typeGroups = {
  string: { icon: '📋', label: 'Campos' },
  array: { icon: '📊', label: 'Tabelas' },
  chart: { icon: '📈', label: 'Gráficos' },
  section: { icon: '📐', label: 'Seções' },
  image: { icon: '🖼️', label: 'Recursos' },
}
```

### Status dos Campos
- `mapped`: campo tem binding confirmado pelo pipeline E pelo operador
- `unmapped`: campo não tem binding — precisa de ação manual
- `unconfirmed`: campo foi auto-mapeado pelo pipeline mas operador ainda não confirmou
- Drag-to-bind (arrastar campo para template) vem no Epic 7 (story 7.3) — por enquanto apenas exibição e clique

### FRs Referenciados
- FR8: Mapeamento de campos XSD com status visual

### Testing
- Vitest for unit tests
- Test files alongside source: `*.spec.ts` or `*.test.ts`

## File List
- `frontend/src/types/field-navigator.types.ts` — Tipos FieldNavItem, FieldNavType, FieldNavSortKey, TYPE_GROUPS, TYPE_ORDER, STATUS_ORDER (created)
- `frontend/src/molecules/FieldNavItem.vue` — Molecule de item de campo com ícone tipo, status badge, ⚠ opcional, emit select (created)
- `frontend/src/organisms/FieldNavigator.vue` — Organism lista de campos agrupada por tipo com sort, collapse, seleção (created)
- `frontend/src/organisms/FieldNavigator.spec.ts` — 19 testes cobrindo todos os ACs (created)
- `frontend/src/organisms/LeftPanel.vue` — Integração FieldNavigator na aba "Campos", removido placeholder (modified)
- `frontend/src/stores/mapping.ts` — Adicionado fieldNavItems state + setFieldNavItems action (modified)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
| 2026-03-17 | 1.1 | Implementação: FieldNavItem, FieldNavigator com grupos/sort/seleção, integração LeftPanel, 19 testes, build passa | Dex (@dev) |

## QA Results

**Reviewer:** Quinn (Test Architect)
**Date:** 2026-03-17
**Gate:** PASS

### Review Checklist
- Code Quality: PASS — Composition API com TypeScript tipado. Tipos dedicados em `field-navigator.types.ts`. Set mutations com `new Set()` para garantir reatividade Vue 3. Props/emits com tipos explícitos.
- Test Coverage: PASS — 19 testes cobrem AC1 (grupos por tipo), AC2 (ícones/badges), AC3 (⚠ opcional), AC4 (seleção nodeId + binding fallback + CSS class), AC5 (contagem), AC7 (sort nome/status/tipo), collapse, empty state, ProgressBar.
- AC Compliance: PASS — Todos os 8 ACs atendidos. AC6 (status derivado de mappingStore.fieldNavItems) com type dedicado.
- Security: PASS — Nenhum input direto de usuário no componente. Dados vêm de stores internos.
- Performance: PASS — Computed com filtros/sorts sem loops desnecessários. grouping via TYPE_ORDER para ordem determinística.
- Build: PASS — `npm run build` passa sem erros TypeScript.

### Issues Found
Nenhum issue bloqueante.

### Gate Status
PASS. Implementação completa com atomic design correto (molecule FieldNavItem + organism FieldNavigator). AC4 implementado com fallback duplo: nodeId → binding search.

## Architect Review

**Reviewer:** Aria (System Architect)
**Date:** 2026-03-17
**Status:** APPROVED_WITH_NOTES

### Design Assessment
- **Coupling:** LOW. FieldNavigator é consumer de mappingStore, inspectorStore, editorStore, templateStore — dependências explícitas e razoáveis para um navigator de campos. FieldNavItem é molecule pura sem store dependencies.
- **Cohesion:** HIGH. FieldNavigator orquestra grupos, sort e seleção. FieldNavItem renderiza um único campo. Separação de responsabilidades clara.
- **Patterns:** `field-navigator.types.ts` com `TYPE_GROUPS`, `TYPE_ORDER`, `STATUS_ORDER` é o padrão correto para constantes compartilhadas. `Set<FieldNavType>` para `collapsedGroups` com `new Set()` para forçar reatividade — correto para Vue 3 Composition API.
- **Scalability:** Adequado para listas de campos típicas (10-200 campos). Para campos muito grandes, virtualização seria necessária — não um problema para o domínio atual do MVP.

### Issues Found
**[LOW] Seleção não reseta ao trocar Layout Type**
- Ao trocar Layout Type, `mappingStore.fieldNavItems` atualiza mas `selectedFieldPath` local não reseta.
- Severidade: LOW — não causa estado inválido, apenas indicador visual desatualizado até próxima interação.

### Technical Debt Registered
- **TD-6.4-001:** Reset de `selectedFieldPath` ao trocar Layout Type (target Epic 7, Story 7.3).
- **TD-6.4-002:** Virtualização de lista para campos com >500 itens (target Epic 9).

### Decision Log
- **D1:** `fieldNavItems: FieldNavItem[]` adicionado ao `mappingStore` como estado separado do `fields: FieldMapping[]` legacy. Decisão: manter compatibilidade com pipeline existente sem breaking change.
- **D2:** ProgressBar atom reutilizado sem cor dinâmica (verde/amarelo/vermelho). Cor dinâmica é enhancement deferido — não bloqueante para AC5.

### Status
APPROVED_WITH_NOTES. FieldNavigator implementado corretamente com bom design. TD-6.4-001 é low priority para MVP.
