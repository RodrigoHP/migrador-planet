# Story 42.11 — Stage 5 Rendering Pydantic Migration

## Status: InProgress

## Descricao

Completar a migração Pydantic dos 5 arquivos de rendering do Stage 5 que foram adiados na Story 42.6. Esses arquivos recebem `field_mappings` e outros dados como plain dicts via context boundary — a story 42.7 fechou o contrato no boundary; agora é possível tipar os consumers internos.

## Debitos Enderecados

| ID | Debito | Arquivo | LOC | Origem |
|----|--------|---------|-----|--------|
| 42.6-AC4 | `coverage_overlay.py` — plain dicts | stage5_template/ | 323 | Adiado em 42.6 |
| 42.6-AC5a | `html_tree.py` — plain dicts | stage5_template/ | 333 | Adiado em 42.6 |
| 42.6-AC5b | `html_helpers.py` — plain dicts | stage5_template/ | 390 | Adiado em 42.6 |
| 42.6-AC5c | `css_generation.py` — plain dicts | stage5_template/ | 218 | Adiado em 42.6 |
| 42.6-AC5d | `variation_matrix.py` — plain dicts | stage5_template/ | 205 | Adiado em 42.6 |

## Acceptance Criteria

- [ ] AC1: `coverage_overlay.py` — `FieldMappingEntry` usado com acesso tipado (`.block_id`, `.xsd_field_path`, `.confidence`, etc.)
- [ ] AC2: `html_tree.py` — nodes do document tree acessados via `DocumentTreeNode` onde aplicável
- [ ] AC3: `html_helpers.py` — idem; funções helper recebem tipos explícitos em vez de `dict`
- [ ] AC4: `css_generation.py` — `LayoutTypeInfo` e tipos de page config usados com acesso tipado
- [ ] AC5: `variation_matrix.py` — `FieldMappingEntry` e `ClusterIntelligence` usados com acesso tipado
- [ ] AC6: Zero erros mypy novos introduzidos
- [ ] AC7: 835+ testes passam (zero regressões nos arquivos migrados)
- [ ] AC8: Output JSON de `result_assembly.py` idêntico ao anterior (shape preservado)

## Escopo

### IN
- `backend/services/stages/stage5_template/coverage_overlay.py`
- `backend/services/stages/stage5_template/html_tree.py`
- `backend/services/stages/stage5_template/html_helpers.py`
- `backend/services/stages/stage5_template/css_generation.py`
- `backend/services/stages/stage5_template/variation_matrix.py`

### OUT
- `result_assembly.py` (já migrado em 42.6)
- Stage 1, 2, 3, 4
- Lógica de negócio de rendering

## Estimativa

8h

## Dependencias

- Story 42.6 (Stage 4+5 base migrada) — Done
- Story 42.7 (extra=forbid + FieldMappingEntry expandido) — Done
- Story 42.8 (Frontend alignment) — Done

## Riscos

| Risco | Probabilidade | Mitigacao |
|-------|--------------|-----------|
| `html_tree.py` usa campos extras não declarados em `DocumentTreeNode` | MÉDIA | Verificar se todos os campos usados estão no modelo 42.7; adicionar se necessário |
| Output JSON de `result_assembly.py` muda → quebra frontend | ALTA | AC8 obrigatório: snapshot test antes/depois |

## Dev Notes

### Abordagem por arquivo

1. **`variation_matrix.py`** — menor dependência, bom warmup
2. **`css_generation.py`** — principalmente `LayoutTypeInfo` e page dimensions
3. **`html_helpers.py`** — helper functions, bem isolado
4. **`html_tree.py`** — usa `DocumentTreeNode` extensivamente (campos declarados em 42.7)
5. **`coverage_overlay.py`** — usa `FieldMappingEntry` com muitos campos

### Boundary pattern (já estabelecido em 42.6)

```python
# Na entrada das funções, field_mappings vem como list[dict] do context
# Converter no início usando model_validate():
mappings = [FieldMappingEntry.model_validate(m) for m in raw_mappings]
# Usar mappings com acesso tipado dentro da função
# Serializar de volta com model_dump() apenas na saída para o context
```

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-10 | @dev | Story criada para fechar gap de 42.6 (ACs 4 e 5 adiados) |
