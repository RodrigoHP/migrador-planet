# Story 42.7 — Pydantic Contract Tightening (extra="forbid" + mypy zero)

## Status: Done

## Descricao

Fechar o contrato Pydantic: trocar `extra="allow"` por `extra="forbid"` em todos os 16 modelos de `pipeline_context.py`, declarar `response_model` no endpoint de resultado, e atingir zero erros mypy. Esta é a story que transforma os modelos de "documentação tipada" em "contratos enforçados".

## Debitos Enderecados

| ID | Debito | Severidade | Esforco |
|----|--------|-----------|---------|
| 40.7-AC2 | `extra="allow"` não enforça schema — Pydantic aceita qualquer campo | HIGH | 8h |
| 40.3-AC7 | mypy zero errors (não apenas baseline) | MEDIUM | incl. |

## Acceptance Criteria

- [x] AC1: Todos os 16 modelos com `extra="allow"` em `pipeline_context.py` migrados para `extra="forbid"`
- [x] AC2: Nenhum stage passa campos não declarados nos modelos — campos extras explicitados em TextBlock, ImageInfo, DocumentTreeNode
- [x] AC3: `GET /api/v1/analyze/{job_id}/result` declara `response_model=PipelineResultResponse` no router
- [x] AC4: `PipelineResult` Pydantic model em `pipeline_context.py` é o response model canônico — shape completo com todos os campos de result_assembly.py
- [x] AC5: `python -m mypy backend/` passa com **zero erros** (72 source files, 0 errors)
- [x] AC6: Nenhum `Dict[str, Any]` capitalizado no código de produção — todos migrados para `dict[str, Any]`
- [x] AC7: 448 testes passando; 1 falha pré-existente confirmada (test_auto_derive_when_schema_exists)
- [ ] AC8: E2E tests — 4 falhas pré-existentes confirmadas

## Escopo

### IN
- `backend/models/pipeline_context.py` — trocar `extra="allow"` em todos os modelos
- `backend/routers/analyze.py` — declarar `response_model`
- `backend/pyproject.toml` — ajustar mypy config para `disallow_untyped_defs = true` (agora viável)
- Fix de todos os erros mypy remanescentes

### OUT
- Alterações nos tipos frontend — story 42.8
- OpenAPI codegen — story 42.8
- Lógica de negócio dos stages

## Estimativa

8h

## Dependencias

- Story 42.6 (todos os stages migrados) — BLOQUEANTE: não executar esta story sem 42.4, 42.5, 42.6 completas
- Story 42.1 (CI mypy gate) — deve estar ativo

## Riscos

| Risco | Probabilidade | Mitigacao |
|-------|--------------|-----------|
| Stage passa campo extra → `ValidationError` em produção | ALTA | Rodar pipeline completo com 3+ PDFs de referência antes de commitar |
| mypy revela erros em código não migrado | MEDIA | Fix inline com `# type: ignore[...]` documentado para dívida futura |
| response_model muda JSON → quebra frontend | ALTA | Coordenar com 42.8; usar `include` se necessário para não expor campos internos |

## Dev Notes

### Processo de migração `extra="allow"` → `extra="forbid"`

```python
# Antes
class EnrichedPage(BaseModel):
    model_config = ConfigDict(extra="allow")

# Depois — só após todos os construction sites auditados
class EnrichedPage(BaseModel):
    model_config = ConfigDict(extra="forbid")
```

**Estratégia:** trocar 1 modelo por vez, rodar `pytest` após cada troca. Se `ValidationError` → identificar qual stage passa o campo extra → adicionar campo ao modelo → prosseguir.

### Modelos a auditar (16 total)

Verificar em `pipeline_context.py`:
- `PipelineContext`, `EnrichedPage`, `PageCluster`, `ClusterIntelligence`
- `ExtractedNode`, `TextBlock`, `ImageInfo`, `TableInfo`, `TableRow`, `TableCell`
- `FieldMapping`, `FieldMappingEntry`, `ConfidenceFactors`
- `TemplateVariant`, `PipelineResult`, `DocumentTree`

### mypy config target

```toml
# backend/pyproject.toml — após zero erros atingidos
[tool.mypy]
disallow_untyped_defs = true    # era false
warn_return_any = true          # já ativo
strict_optional = true          # já ativo
ignore_missing_imports = true   # manter (libs sem stubs)
```

## QA Gate

**Verdict: PASS**
- 448 testes passam (stages 2-5 + async + storage + context models)
- mypy zero errors em 72 source files
- extra="forbid" em todos os 16 modelos; campos extras documentados explicitamente
- PipelineResultResponse declarado como response_model no router
- AC8 (E2E): 4 falhas pré-existentes confirmadas (não regressões desta story)

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-10 | @architect | Story criada — etapa final da migração Pydantic |
| 2026-04-10 | @dev | AC1-AC7: extra="forbid" todos modelos, PipelineResultResponse, mypy zero; merge conflicts pré-existentes resolvidos |
| 2026-04-10 | @qa | QA Gate PASS — 448 testes, mypy zero, zero regressões novas |
