---
epic: 1
story: 5
title: "Step 4 — Geração"
status: Draft
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [monaco_lazy_test, chartjs_config_test, sse_generation_test, preview_tab_test, bundle_size_check]
---

# Story 1.5: Step 4 — Geração

## Status

Draft

## Story

**As a** Operador Técnico,
**I want** ver o HTML/CSS/JS gerado no Monaco Editor com 3 abas, acompanhar o progresso de geração via SSE, abrir um preview em nova aba e configurar gráficos Chart.js,
**so that** possa revisar e editar o código gerado antes de exportar o ZIP final.

## Acceptance Criteria

1. Rota `/geracao` renderiza `GeracaoPage` com dois painéis: informações/controles (35%) e Monaco/preview (65%)
2. Ao entrar: `POST /generate` disparado automaticamente com dados do `layoutStore` e `mappingStore`; `generation.jobId` preenchido
3. `useSSE(generation.previewJobId)` monitora progresso; `ProgressBar` visível durante geração; `MonacoTabs` oculto durante processamento
4. Após geração: `generation.html`, `generation.css`, `generation.js` populados; `MonacoTabs` exibe 3 abas (`index.html`, `style.css`, `base.js`) com Monaco Editor (lazy import)
5. `FidelityScore` atom exibe score 0-100 com cor semântica: verde ≥80, amarelo 60-79, vermelho <60; animação contagem 0→valor em 600ms no mount
6. `IASuggestionList` molecule lista sugestões da IA clicáveis (`generation.iaSuggestions`)
7. `RightPanelToggle` molecule permite alternar entre `html-preview`, `monaco`, `wysiwyg`, `chartjs-config`
8. Botão "Abrir Preview": `window.open(${API_BASE}/preview/${generation.previewJobId}, '_blank')`; se `generation.previewExpired: true`, exibe aviso e botão "Regenerar"
9. `ChartjsConfigPanel` organism: formulário de configuração + canvas Chart.js para preview em tempo real; salva em `generation.chartConfigs`
10. Monaco é editável; edições salvas em `generation.monacoEdits`; botão "Regenerar com edições" disponível
11. Fidelidade < 70%: warning banner não bloqueante
12. Monaco e Chart.js carregam via `defineAsyncComponent` / lazy import (fora do chunk principal)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] Task 1 — Criar `GeracaoPage.vue` com dois painéis (AC: 1)
  - [ ] Layout CSS: dois painéis `35% | 65%`
  - [ ] Registrar rota `/geracao` com guard: `layoutStore.confirmed === true`

- [ ] Task 2 — Disparar POST /generate no mount (AC: 2)
  - [ ] `onMounted`: chamar `POST /generate` com payload de `mappingStore.fields` + `layoutStore.$state`
  - [ ] Resposta: `{ jobId: string }` → `generation.previewJobId = jobId`
  - [ ] Ativar `useSSE(generation.previewJobId)`

- [ ] Task 3 — SSE progresso de geração (AC: 3)
  - [ ] Reutilizar `useSSE` composable da Story 1.2
  - [ ] Durante processamento: `ProgressBar` + `ProgressLabel` visíveis, `MonacoTabs` oculto com `v-if`
  - [ ] Ao `status === 'done'`: esconder progress, mostrar MonacoTabs + FidelityScore

- [ ] Task 4 — Criar `MonacoTabs` organism com lazy import (AC: 4, 10, 12)
  - [ ] `defineAsyncComponent(() => import('./MonacoTabsInner.vue'))`
  - [ ] 3 abas: `index.html`, `style.css`, `base.js`
  - [ ] Cada aba: Monaco Editor com linguagem correspondente (html, css, javascript)
  - [ ] Monaco editável; ao editar: `generation.monacoEdits[tab] = value`
  - [ ] Tab switch sem animação (0ms)

- [ ] Task 5 — Criar Atom `FidelityScore` (AC: 5)
  - [ ] Prop `score: number`
  - [ ] Cor: `score >= 80` → verde, `60-79` → amarelo, `< 60` → vermelho
  - [ ] Animação: `0 → score` em 600ms no mount via `requestAnimationFrame`

- [ ] Task 6 — Criar Molecule `IASuggestionList` (AC: 6)
  - [ ] Prop `suggestions: IASuggestion[]`
  - [ ] Cada item: ícone de tipo + mensagem + botão de ação (se `action` definido)

- [ ] Task 7 — Criar Molecule `RightPanelToggle` (AC: 7)
  - [ ] 4 botões: HTML Preview, Monaco, WYSIWYG, Chart.js Config
  - [ ] Botão ativo destacado; emite `update:modelValue`
  - [ ] Controla `generation.rightPanel`

- [ ] Task 8 — Implementar botão "Abrir Preview" (AC: 8)
  - [ ] `window.open(\`${API_BASE}/preview/${generation.previewJobId}\`, '_blank')`
  - [ ] Se `generation.previewExpired`: banner de aviso + botão "Regenerar Preview"
  - [ ] TTL tracking: setar `generation.previewExpired = true` após 10min via `setTimeout`

- [ ] Task 9 — Criar `ChartjsConfigPanel` organism com lazy Chart.js (AC: 9, 12)
  - [ ] Lazy import: `const Chart = await import('chart.js')`
  - [ ] Formulário: tipo de gráfico, dataField, labelField, cores, título
  - [ ] Canvas para preview em tempo real
  - [ ] Ao salvar: `generation.chartConfigs[chartId] = config`

- [ ] Task 10 — Warning de fidelidade baixa (AC: 11)
  - [ ] Computed `isFidelityLow`: `generation.fidelityScore < 70`
  - [ ] Banner amarelo não bloqueante com `v-if="isFidelityLow"`

## Dev Notes

### Generation Store — Interfaces
[Source: docs/front-end-spec.md#9-stores-pinia-e-composables]
```typescript
interface GenerationStore {
  html: string | null; css: string | null; js: string | null; exemplo: string | null
  fidelityScore: number | null; fidelityComment: string | null
  iaSuggestions: IASuggestion[] | null
  monacoEdits: { html?: string; css?: string; js?: string }
  chartConfigs: Record<string, ChartjsConfig>
  previewJobId: string | null; previewExpired: boolean
  rightPanel: 'html-preview' | 'monaco' | 'wysiwyg' | 'chartjs-config'
  activeChartId: string | null
}
```

### API Endpoints
[Source: docs/front-end-spec.md#12-integracao-backend]
```
POST ${API_BASE}/generate          // payload: { mappingFields, layoutConfig }
GET  ${API_BASE}/progress/{jobId}  // SSE
GET  ${API_BASE}/preview/{jobId}   // nova aba (10min TTL)
```

### Monaco Editor — Lazy Import
[Source: docs/front-end-spec.md#11-performance]
```typescript
// Em vite.config.ts — já configurado em Story 1.1:
// manualChunks: { 'monaco': ['monaco-editor'] }

// No componente (lazy):
const MonacoEditor = defineAsyncComponent(() =>
  import('monaco-editor').then(monaco => /* setup */)
)
```

### Chart.js — Lazy Import
[Source: docs/front-end-spec.md#11-performance]
```typescript
// Em vite.config.ts — já configurado em Story 1.1:
// manualChunks: { 'chartjs': ['chart.js'] }

// No componente:
const { Chart, registerables } = await import('chart.js')
Chart.register(...registerables)
```

### FidelityScore — Animação
[Source: docs/front-end-spec.md#10-animacoes-e-micro-interacoes]
```typescript
// Animação contagem 0 → valor em 600ms
// Respeitar prefers-reduced-motion: pular animação se ativo
```

### EventSource cleanup
[Source: docs/front-end-spec.md#11-performance]
```typescript
// useSSE já fecha em onUnmounted
// Mas também fechar quando status === 'done'
```

### Dependências de Stories Anteriores
- Story 1.1: `useSSE` composable, `generation store`, `RightPanelToggle` molecule
- Story 1.4: `layoutStore.$state` disponível para payload do /generate

### Testing (risco ALTO — story mais complexa)
- Verificar Monaco e Chart.js NÃO estão no chunk principal (bundle analyzer)
- Testar SSE fecha ao receber `done`
- Testar `previewExpired` após 10min (simular com timeout menor no dev)
- Testar edição Monaco e regeneração

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Story criada | @sm (River) |

## Dev Agent Record

### Agent Model Used
_A ser preenchido pelo @dev_

### Completion Notes
_A ser preenchido pelo @dev_

### File List
_A ser preenchido pelo @dev_

## QA Results
_A ser preenchido pelo @qa_
