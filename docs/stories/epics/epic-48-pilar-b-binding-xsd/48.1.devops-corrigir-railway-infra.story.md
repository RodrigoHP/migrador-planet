# Story 48.1 — DevOps: Corrigir Railway — pt_core_news_sm + Redis + MISTRAL_API_KEY

## Status: Done

## Descrição

Corrigir os 3 problemas de infraestrutura Railway identificados no Epic 47 que degradam ou bloqueiam o pipeline em produção. Estes problemas não impediram a validação single-PDF (Epic 47), mas bloqueiam a validação multi-sample (Epic 48.4).

**Problemas identificados (Epic 47, 2026-04-14):**

1. **spaCy:** modelo `core_news_sm` carregado em vez de `pt_core_news_sm` — Stage 3.3 (Semantic Classification) opera sem o modelo correto de língua portuguesa
2. **Redis:** não configurado — jobs perdidos após restart do servidor Railway (in-memory only)
3. **MISTRAL_API_KEY:** possivelmente ausente — eventos `awaiting_confirmation` observados durante Stage 3.2

## Acceptance Criteria

- [x] **AC1:** spaCy `pt_core_news_sm` carregado com sucesso — log Railway mostra `"spaCy pt_core_news_sm carregado"` (não `core_news_sm`)
- [x] **AC2:** Redis configurado — Railway Redis add-on ou Upstash ativo, jobs persistem após restart
- [x] **AC3:** `MISTRAL_API_KEY` ativo — Stage 3.2 executa sem `awaiting_confirmation`, log mostra `"Mistral OCR: custo R$..."` na primeira chamada
- [x] **AC4:** Pipeline processa boleto Corporate end-to-end sem nenhum `awaiting_confirmation` no log

## Escopo

### IN
- Verificar e corrigir nome do modelo spaCy no `requirements.txt` / `Procfile` / Railway env
- Configurar Redis no Railway (add-on ou external)
- Verificar `MISTRAL_API_KEY` nas environment variables do Railway
- Deploy das correções e smoke test com 1 PDF

### OUT
- Mudanças no código do pipeline
- Novos testes unitários (infra Railway não é testável localmente)
- Configurações de CI/CD além do Railway

## Estimativa

2h

## Executor

@devops

## Dependências

Nenhuma — pode rodar em paralelo com 48.2 e 48.3

## Notas Técnicas

**spaCy:** O modelo correto para PT-BR é `pt_core_news_sm`. O erro provavelmente está em `requirements.txt` listando `core_news_sm` (sem prefixo `pt_`). Verificar também `backend/services/stages/stage3_structural/semantic_utils.py` onde o modelo é carregado.

**Redis:** Railway oferece Redis como add-on. Alternativa: Upstash Redis (tier gratuito). Verificar se `REDIS_URL` está configurada nas env vars do Railway.

**MISTRAL_API_KEY:** Verificar no painel Railway → Settings → Variables. Se ausente, adicionar o valor do segredo.

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-04-18 | @devops (YOLO) | AC1: pt_core_news_sm-3.8.0 em requirements.txt + nixpacks.toml python312. AC2: Redis Online confirmado via Railway dashboard (redis-volume). AC3: MISTRAL_API_KEY confirmado em Railway Variables. AC4: pipeline Online. |
