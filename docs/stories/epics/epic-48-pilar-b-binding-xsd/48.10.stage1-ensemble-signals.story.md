# Story 48.10 — Stage 1: Ensemble Voting Signals — pHash + Font Jaccard + Struct Distance

## Status: Done

## Descrição

Implementar os 4 sinais calibrados (48.9 + 48.11) no Stage 1 para substituir a fórmula atual
`0.8 * geometry + 0.2 * density` por ensemble voting estrutura-puro.

O spike 48.7 confirmou: a fórmula atual agrupa 3 instâncias do mesmo template em 3
clusters separados. Os thresholds calibrados no 48.9 permitem detectar corretamente
"mesmo template" vs "templates distintos" usando sinais invariantes a conteúdo dinâmico.

## Acceptance Criteria

- [x] **AC1:** Novo módulo `backend/services/stages/stage1_clustering/signals.py` com 4 funções:
  `masked_phash(page)`, `font_signature(page)`, `struct_sequence(blocks)`, `markdown_fingerprint(page)` (ver 48.11)
- [x] **AC2:** `PageInfo` estendido com campos opcionais `phash`, `font_sig`, `struct_seq`, `md_hash`, preenchidos durante `_extract_blocks`
- [x] **AC3:** Nova função `_ensemble_similarity(pi_a, pi_b)` em `clustering_algorithms.py` — retorna float em [0,1] baseado em voting majoritário (escala 4 sinais):
  - 4/4 sinais → 0.97
  - 3/4 sinais → 0.90
  - 2/4 sinais → 0.75
  - 1/4 sinais → 0.35
  - 0/4 sinais → 0.05
  - Sinal `md_hash` é one-sided: match=SAME vote, mismatch=abstém (não penaliza)
- [x] **AC4:** `_compute_similarity` usa `_ensemble_similarity` em vez de `geo*0.8 + den*0.2`; fallback para geometria quando ensemble retorna -1.0
- [x] **AC5:** 19 testes unitários em `backend/tests/test_stage1_ensemble_signals.py` validando: SAME pair → sim>=0.82, DIFF pair → sim<0.82, md_hash abstém em mismatch, fallback com 0 sinais → -1.0
- [x] **AC6:** `make test` verde (unit tier) — todos 19 testes passam

## Escopo

### IN

- Novo arquivo `signals.py` (funções puras, sem side-effects)
- Modificação mínima em `page_preprocessing.py`: campos opcionais `phash` e `font_sig` em `PageInfo`, preenchidos no `_extract_blocks`
- Modificação em `clustering_algorithms.py`: `ensemble_similarity()` + wiring em `_compute_similarity`
- Testes unitários com mocks de `PageInfo` (sem fitz real)
- Atualizar `ClusteringConfig`: adicionar `T_phash=16`, `T_font=0.47`, `T_struct=0.65`

### OUT

- Union-Find clustering (substituto do NetworkX) — stories futuras
- Checkpoint infra (Supabase) — stories futuras
- pymupdf4llm markdown hash — stories futuras (instalar primeiro)
- Modificações em Stage 3, 4, 5

## Estimativa

1 dia (8h)

## Executor

@dev · Quality Gate: @qa

## Dependências

- Story 48.9 ✅ — thresholds calibrados disponíveis em `calibration-thresholds.json`
- `imagehash` ✅ (já no stack, confirmado em cluster_validation.py)

## Notas Técnicas

**Thresholds calibrados (48.9):**
```python
T_phash  = 16   # hamming distance <= 16 → SAME signal
T_font   = 0.47 # Jaccard similarity >= 0.47 → SAME signal
T_struct = 0.65 # edit distance <= 0.65 → SAME signal
```

**Scores ensemble → similarity float (4-signal scale, calibrado 48.9/48.11):**
```python
ENSEMBLE_SCORES = {4: 0.97, 3: 0.90, 2: 0.75, 1: 0.35, 0: 0.05}
```

**Fallback quando phash/font_sig ausentes:**
Se `pi.phash is None` ou `pi.font_sig is None`, sinal é ignorado e voting é feito apenas
com os sinais disponíveis. Com 0 sinais disponíveis → volta para `geometry_similarity`.

**Masked pHash — regra de mascaramento:**
Mascarar APENAS blocos de texto (type=0). Blocos de imagem (logos, barcodes) SÃO
incluídos na thumbnail — são parte da identidade visual do template.

**Performance:** `_extract_blocks` já abre o PDF uma vez. Adicionar pHash e font_sig
nesse momento não abre PDFs extras. Impacto estimado: +50-100ms por página.

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-04-18 | @sm (YOLO) | Story criada — ensemble signals Stage 1 |
| 2026-04-18 | @dev (YOLO) | Implementado: signals.py (4 funções), PageInfo+4 campos, _ensemble_similarity 4-signal, _compute_similarity wired, 19 unit tests. Gap 1 Stage 1 confirmado corrigido via spike_48_validate_stage1_local.py (7/7 casos). |
| 2026-04-18 | @qa (YOLO) | Gate: PASS — 4 sinais implementados, thresholds T_phash=16/T_font=0.47/T_struct=0.65, 19 testes verdes, solução genérica validada em 6 tipos de template |
