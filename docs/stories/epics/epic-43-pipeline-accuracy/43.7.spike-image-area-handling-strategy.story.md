# Story 43.7 — SPIKE: Classificação de Tipo de Conteúdo Raster em image_area

## Status: Draft

## Descrição

Quando o pipeline encontra uma região raster em um PDF, precisa saber **o que é** antes de decidir como processar. O `visual_analysis.py` já usa GPT-4o para detecção visual, mas `section_utils.py` descarta `image_area` silenciosamente (linha 469) — em parte porque ainda não há uma estratégia definida de classificação de tipo de conteúdo raster.

O tipo determina o handler:

| Tipo detectado | Handler correto |
|----------------|----------------|
| `barcode` | zxing-cpp decode (já existe) |
| `table` | Mistral OCR (decidido em 43.3) |
| `chart` | preservar crop como imagem |
| `logo` / `image` | preservar crop como imagem |

O problema: um mesmo elemento pode chegar ao pipeline como `image_area` genérico (quando o detector não tem certeza) ou ser misclassificado. A spike avalia **qual modelo/abordagem classifica o tipo de conteúdo raster com maior precisão e menor custo**, usando os fixtures e o runner já criados em 43.3.

O `image_classification_ground_truth.json` existente (barcode crop classificado como barcode) é o ponto de partida — os modelos já foram testados nesse cenário em 43.3. Precisamos expandir com mais tipos e formalizar a recomendação.

## Acceptance Criteria

- [ ] **AC1 — Ground truth expandido:**
  - Adicionar ≥2 crops adicionais de tipos distintos ao conjunto de fixtures: um `chart` e um `logo` extraídos dos PDFs de amostra disponíveis
  - Criar ou atualizar `backend/tests/fixtures/ocr_bakeoff/image_classification_ground_truth.json` com todos os casos: `barcode`, `chart`, `logo` (≥3 samples no total)
  - Cada entry: `crop_file`, `expected_type`, `expected_render_strategy`, `notes`

- [ ] **AC2 — Runner executado contra os candidatos:**
  - Adaptar `backend/scripts/spike_ocr_bakeoff.py` ou criar `spike_image_classification_bakeoff.py`
  - Candidatos a testar:
    - **GPT-4o Vision** (via OpenRouter, ~$0.02/call — já ativo no pipeline)
    - **Gemini 2.0 Flash** (via OpenRouter, ~$0.0003/call)
    - **Regra heurística** (aspect ratio + histograma de cor via PIL — custo $0)
  - Métricas por candidato: accuracy, latência p50, custo estimado por PDF (assumindo 3 regiões raster/página)

- [ ] **AC3 — Avaliação de degradação aceitável:**
  - Comparar: GPT-4o (baseline de precisão) vs Gemini Flash vs heurística
  - Determinar se há degradação de accuracy ao trocar GPT-4o por Gemini ou heurística
  - Documentar quais tipos de erro são aceitáveis (e.g., logo classificado como image → mesmo handler, sem impacto)

- [ ] **AC4 — Relatório de recomendação:**
  - Criar `docs/reports/epic-43-ocr-bakeoff/image-classification-report.md` com:
    - Tabela comparativa de candidatos (accuracy × custo × latência)
    - Análise de erro por tipo (quais misclassificações são inofensivas?)
    - Recomendação final: qual candidato adotar para classificar `image_area`
    - Decisão sobre fallback: o que fazer quando confidence < threshold

- [ ] **AC5 — Gap de implementação documentado:**
  - Esboçar a mudança necessária em `section_utils.py` para incluir `image_area` na função `_assign_visual_elements_to_sections`
  - Abrir story 43.8 ou confirmar que story existente cobre a implementação

## Escopo

### IN
- `backend/tests/fixtures/ocr_bakeoff/image_classification_ground_truth.json` — ATUALIZADO: ≥3 samples (barcode + chart + logo)
- `backend/tests/fixtures/ocr_bakeoff/image_area_chart_sample.png` — CRIADO: crop de gráfico real
- `backend/tests/fixtures/ocr_bakeoff/image_area_logo_sample.png` — CRIADO: crop de logo real
- `backend/scripts/spike_image_classification_bakeoff.py` — CRIADO: runner para comparar candidatos
- `docs/reports/epic-43-ocr-bakeoff/image-classification-report.md` — CRIADO: relatório final

### OUT
- Implementação em `section_utils.py` (reservada para 43.8)
- Mudanças em `visual_analysis.py`
- Avaliação de outros tipos (`svg_area`, `chart_area`) — já tratados

## Estimativa

4h (spike empírico)

## Dependências

- 43.3 Done ✓ (runner, fixtures e padrão de relatório existem)
- `backend/scripts/spike_ocr_bakeoff.py` disponível como base
- `backend/tests/fixtures/ocr_bakeoff/image_classification_ground_truth.json` existe (barcode sample)
- PDFs de amostra com charts e logos disponíveis localmente

## Dev Notes

### Gap atual em section_utils.py

```python
# backend/services/stages/stage3_structural/section_utils.py — linha 469
if rtype not in ("chart_area", "barcode_area", "svg_area", "table_area"):
    continue  # image_area descartado silenciosamente
```

### Por que a classificação de tipo importa

O tipo determina o handler downstream — não é apenas metadata:

```
barcode  → zxing-cpp → valor decodificado → campo dinâmico
table    → Mistral OCR → estrutura de tabela → grid no template
chart    → crop preservado → <img> estático/dinâmico
logo     → crop preservado → <img> estático
```

Misclassificar `barcode` como `logo` → barcode não decodificado → campo XSD não mapeado.
Misclassificar `chart` como `table` → Mistral tenta OCR de gráfico → output incorreto.

### Hipótese sobre heurística

Charts têm distribuição de cores muito diferente de barcodes (variadas vs preto/branco). Logos tendem a ter áreas de cor sólida com bordas bem definidas. Uma heurística simples via PIL (histograma de cor + razão aspecto) pode distinguir os casos mais comuns sem LLM — a ser testado empiricamente.

### Resultado esperado dos modelos em 43.3 (referência)

Na 43.3, todos os modelos testados identificaram corretamente o barcode crop como "barcode" (não logo). A extensão da ground truth para chart e logo vai validar se esse padrão se mantém.

### Como extrair crops adicionais

```python
import fitz

doc = fitz.open("Corporate.Boleto.Convenio.pdf")
page = doc[0]
# Usar bbox da visual_analysis para recortar região de chart/logo
rect = fitz.Rect(x0, y0, x1, y1)
pix = page.get_pixmap(clip=rect, dpi=150)
pix.save("image_area_chart_sample.png")
```

## Dev Agent Record

### Agent Model Used
_a preencher_

### Completion Notes
_a preencher_

### File List
_a preencher_

## QA Results
_a preencher_

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-13 | @aios-master (Orion) | Story criada — spike para avaliar classificação de tipo de conteúdo raster (barcode/chart/logo): GPT-4o vs Gemini Flash vs heurística PIL; gap documentado em section_utils.py:469 |
