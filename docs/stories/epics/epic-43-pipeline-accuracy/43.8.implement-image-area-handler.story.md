# Story 43.8 — Implementar Handler de image_area em section_utils.py

## Status: Done

## Descrição

Implementar o handler de `image_area` em `_assign_visual_elements_to_sections` (section_utils.py:469) com base na decisão da spike 43.7: heurística PIL como classificador primário.

**Decisão da 43.7:** Heurística PIL — `aspect_ratio > 3.0 AND pct_bw > 85%` → barcode; caso contrário → preserve_as_image_crop. Accuracy 3/3 (100%) no domínio Planet Express. Custo $0.

## Acceptance Criteria

- [x] **AC1:** `_assign_visual_elements_to_sections` inclui `image_area` no conjunto de tipos processados (linha 469)
- [x] **AC2:** Função `_classify_image_area_heuristic(bbox, page_image)` implementada com regra PIL (aspect + pct_bw)
- [x] **AC3:** Se heurística → barcode: adicionar em `section["barcodes"]` com `source="image_area_refined"`
- [x] **AC4:** Se heurística → logo/image: adicionar em `section["images"]` com `render_strategy="preserve_as_image_crop"`
- [x] **AC5:** Testes unitários `@pytest.mark.unit` cobrindo: barcode crop → barcodes[], logo crop → images[]
- [x] **AC6:** `make test` passa sem regressões — 297 unit tests PASS

## Escopo

### IN
- `backend/services/stages/stage3_structural/section_utils.py` — MODIFICADO
- `backend/tests/test_section_utils.py` (ou equivalente) — MODIFICADO/CRIADO

### OUT
- Mudanças em `visual_analysis.py`
- Stage 5 / template engine
- Outros tipos (`svg_area`, `chart_area`)

## Estimativa

4h

## Dependências

- 43.7 Done ✓ (decisão de estratégia + heurística documentada)

## Dev Notes

Ver `docs/reports/epic-43-ocr-bakeoff/image-classification-report.md` para detalhes da implementação sugerida e esboço do código.

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6 (Dex)

### Completion Notes
- AC1: `"image_area"` adicionado ao filtro de tipos em `_assign_visual_elements_to_sections` (section_utils.py:508)
- AC2: `_classify_image_area_heuristic(bbox, screenshot_path)` implementada — PIL crop + aspect ratio + pct_bw + unique_colors. Fallback para "logo" quando screenshot ausente ou PIL falha.
- AC3: barcode → `section["barcodes"]` com `source="image_area_refined"`
- AC4: logo → `section["images"]` com `render_strategy="preserve_as_image_crop"` e `image_type="logo"`
- AC5: 9 testes unitários em `test_stage3_image_area_handler.py` — incluindo edge case logo_cedente (aspect 3.2, pct_bw 67.7%)
- AC6: 297 unit tests PASS (288 anteriores + 9 novos)
- `_assign_visual_elements_to_sections` recebe `screenshot_path: str | None = None` (backward compatible)
- Chamada em tree_builder.py:118 atualizada para passar `page_data.get("screenshot_path")`

### File List
- `backend/services/stages/stage3_structural/section_utils.py` — MODIFICADO: `_classify_image_area_heuristic` + handler image_area + assinatura `screenshot_path`
- `backend/services/stages/stage3_structural/tree_builder.py` — MODIFICADO: passa `screenshot_path` na chamada
- `backend/services/stages/stage3_structural/__init__.py` — MODIFICADO: exporta `_classify_image_area_heuristic`
- `backend/tests/test_stage3_image_area_handler.py` — CRIADO: 9 testes unitários

## QA Results

**Verdict: PASS**
**Reviewed by:** @qa (Quinn) — 2026-04-13

| AC | Verificação | Status |
|----|------------|--------|
| AC1 | `"image_area"` no filtro — não descartado silenciosamente | ✅ PASS |
| AC2 | `_classify_image_area_heuristic` — regra PIL correta, fallback seguro | ✅ PASS |
| AC3 | barcode → `section["barcodes"]` com `source="image_area_refined"` | ✅ PASS |
| AC4 | logo → `section["images"]` com `render_strategy="preserve_as_image_crop"` | ✅ PASS |
| AC5 | 9 testes `@pytest.mark.unit` — edge case logo_cedente incluído | ✅ PASS |
| AC6 | 297 unit tests PASS — zero regressões | ✅ PASS |

LOW: `screenshot_path` keyword-optional — backward compatible.

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-13 | @dev (Dex) | Story criada como AC5 da 43.7 — implementação do handler de image_area |
| 2026-04-13 | @dev (Dex) | Implementação completa. AC1-AC6 PASS. 9 testes. 297 unit PASS. Status→InReview. |
| 2026-04-13 | @qa (Quinn) | QA Gate: PASS. Todos ACs verificados. Zero regressões. Status→Done. |
