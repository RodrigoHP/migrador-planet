# Story 45.1 — Test Tier Separation: Markers + Makefile

## Status: Ready

## Descrição

A suite de testes atual (903 testes) leva 7h14m para rodar porque não existe separação entre testes rápidos (lógica pura) e lentos (pipeline real com fitz/pdfplumber). Isso bloqueia o dev loop: mudanças de 2 linhas requerem 7h de espera para validação de regressão.

Esta story adiciona markers pytest (`unit`, `integration`, `benchmark`) em todos os arquivos de teste e cria targets Makefile para rodar cada tier separadamente. Resultado: `make test` (unit) em < 2 minutos para uso no dev loop.

**Impacto medido:** Story 43.1 (2 linhas de código) bloqueou sessão de 7h14m esperando suite completa.

## Acceptance Criteria

- [ ] **AC1**: `pyproject.toml` registra 3 markers com descrições:
  - `unit`: testes sem I/O externo, < 500ms cada
  - `integration`: testes com fitz/pdfplumber/httpx, < 120s cada
  - `benchmark`: testes de performance com timeout >= 60s
  - Adicionar `filterwarnings` para `PytestUnknownMarkWarning` como erro (força uso dos markers)

- [ ] **AC2**: Todos os arquivos de teste anotados com o marker correto. Critério objetivo:
  - `unit`: sem `fitz`, sem `pdfplumber`, sem `httpx.AsyncClient`, sem `TestClient`, sem `timeout(>=60)`
  - `integration`: usa `fitz` OU `pdfplumber` OU `TestClient` OU `AsyncClient` OU pipeline async completo
  - `benchmark`: `@pytest.mark.timeout(>=60)` explícito
  - Mapeamento inicial (pode ser refinado após medição):

  ```
  UNIT:
    test_stage3_smart_classify.py, test_xsd_parsing.py, test_auto_fix_rules.py,
    test_pipeline_context_models.py, test_pipeline_stage_contracts.py,
    test_rate_limiting.py, test_redis_job_store.py, test_redis_resilience.py,
    test_ttl_disk_cleanup.py, test_validation_template.py,
    test_xsd_synthetic_generator.py, test_openrouter_cost_tracking.py

  INTEGRATION:
    test_pipeline_e2e.py, test_pipeline_orchestrator_v2.py,
    test_stage1_layout_clustering.py, test_stage1_stage2_integration.py,
    test_stage2_deep_extraction.py, test_stage3_structural_analysis.py,
    test_stage3_stage4_integration.py, test_stage4_field_mapping.py,
    test_stage5_template_generation.py, test_data_security_chain.py,
    test_upload_unified.py, test_upload_validation.py, test_uuid_validation.py,
    test_storage_gateway_integration.py, test_generate.py,
    test_multipdf_pipeline.py, test_async_state_unification.py,
    test_operational_improvements.py, test_db_quick_wins.py,
    test_analyze_router.py, test_storage_gateway.py

  BENCHMARK:
    test_pipeline_benchmark.py
  ```

- [ ] **AC3**: `backend/Makefile` criado com targets:
  ```makefile
  test:              ## Dev loop — unit only (< 2 min)
  test-integration:  ## Pre-push — unit + integration (< 35 min)
  test-full:         ## CI nightly — tudo incluindo benchmark
  test-stage N:      ## Smoke por stage (ex: make test-stage 3)
  ```

- [ ] **AC4**: `pytest -m unit -q` roda em < 2 minutos (validar com `time`)

- [ ] **AC5**: `pytest --markers` lista os 3 markers registrados sem warnings

- [ ] **AC6**: Nenhum teste sem marker — `pytest --co -q` não retorna warnings de mark desconhecida

## Escopo

### IN
- `backend/pyproject.toml` — adicionar `markers` e `filterwarnings`
- `backend/tests/test_*.py` — anotar cada classe/função com `@pytest.mark.{tier}`
- `backend/Makefile` — criar targets por tier

### OUT
- Refatoração de fixtures (Story 45.2)
- pytest-xdist (Story 45.2)
- CI/CD pipeline separation (Story 45.2 ou futuro)
- Modificação da lógica dos testes

## Estimativa

4h

## Dependências

- Nenhuma dependência de outras stories

## Dev Notes

### Estratégia de Anotação

Anotar no nível de **classe** quando o arquivo tem uma classe por tier. Anotar no nível de **arquivo** via `pytestmark` quando todos os testes do arquivo são do mesmo tier:

```python
# Para arquivo inteiramente unit:
import pytest
pytestmark = pytest.mark.unit

# Para arquivo inteiramente integration:
pytestmark = pytest.mark.integration

# Para arquivo misto (raro):
class TestUnitPart:
    @pytest.mark.unit
    def test_algo(self): ...

class TestIntegrationPart:
    @pytest.mark.integration
    def test_algo(self): ...
```

### pyproject.toml — configuração

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "unit: testes sem I/O externo, rodam em < 500ms cada",
    "integration: testes com fitz/pdfplumber/httpx, rodam em < 120s cada",
    "benchmark: testes de performance com timeout >= 60s",
]
filterwarnings = [
    "error::pytest.PytestUnknownMarkWarning",
]
```

### Makefile — template

```makefile
.PHONY: test test-integration test-full

test:
	cd backend && python -m pytest -m unit -q

test-integration:
	cd backend && python -m pytest -m "unit or integration" -q

test-full:
	cd backend && python -m pytest -q

test-stage:
	cd backend && python -m pytest -m unit -k "stage$(N)" -q
```

### Atenção: test_stage3_structural_analysis.py

2279 linhas, 71 testes. Não usa `fitz`/`pdfplumber` diretamente. Classificar como `integration` inicialmente (executa Stage 3 completo com dados sintéticos). Após medição com `--durations`, pode migrar para `unit` se < 2 min total.

### Validação de AC4

```bash
cd backend && time python -m pytest -m unit -q
# Esperado: < 2 minutos wall clock
```

## Dev Agent Record

### Agent Model Used
—

### Completion Notes
—

### File List
—

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-13 | @sm (River) | Story criada — diagnóstico sessão 43.1 (7h14m por suite completa) |
| 2026-04-13 | @po (Pax) | Validação GO — critérios testáveis, escopo bem delimitado |
