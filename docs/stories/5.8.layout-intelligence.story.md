---
epic: 5
story: 8
title: "Bloco 3: Layout Intelligence — Alinhamento, Multi-Example, Estabilidade e Variantes"
status: Ready for Review
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [static_analysis, api_test]
depends_on: [5.7]
---

# Story 5.8: Bloco 3: Layout Intelligence — Alinhamento, Multi-Example, Estabilidade e Variantes

## Status
Ready for Review

## Story
**As a** bloco do pipeline,
**I want** alinhar layouts entre PDFs, analisar múltiplos exemplos para distinguir labels de valores dinâmicos, classificar estabilidade de elementos e detectar variantes,
**so that** o motor de matching tenha inteligência estrutural de alta qualidade.

## Acceptance Criteria
1. Stage 8 — Layout Alignment: normalizar diferenças de coordenadas entre múltiplos PDFs; com 1 PDF: auto-align (trivial)
2. Stage 9 — Multi-Example Analysis: comparar documentos para detectar labels (texto que não muda) vs valores dinâmicos (texto que muda); com 1 PDF: Vision AI assume inferência
3. Stage 10 — Stability Classification: classificar cada elemento como stable (mesma posição/conteúdo em todos docs), variable (mesma posição, conteúdo diferente), ou absent (ausente em alguns docs)
4. Stage 11 — Variant Detection: detectar campos opcionais (presentes em alguns docs), seções condicionais (grupo de campos com mesmo padrão de presença), tabelas dinâmicas (contagem variável de linhas)
5. Stage 12 — Normalization: normalizar padrões detectados em metadados estruturados para matching
6. Todos os 5 estágios registrados no orchestrator, emitindo SSE progress
7. Output enriquece LayoutTypes com: labels[], dynamic_fields[], stability_map, variants[], conditional_sections[]
8. Com 1 PDF: estágios executam com confiança reduzida, flag "single document — reduced accuracy"
9. Testes cobrem: detecção label vs valor, classificação de estabilidade, detecção de variantes, fallback de 1 PDF

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [x] Task 1 — Implementar Layout Alignment (AC: 1)
  - [x] Criar `backend/services/stages/layout_alignment.py`
  - [x] Com múltiplos PDFs: calcular offsets de coordenadas entre documentos do mesmo cluster
  - [x] Normalizar posições para sistema de coordenadas comum
  - [x] Com 1 PDF: auto-align trivial (sem transformação necessária)
  - [x] Registrar no StageRegistry

- [x] Task 2 — Implementar Multi-Example Analysis (AC: 2)
  - [x] Criar `backend/services/stages/multi_example_analysis.py`
  - [x] Com múltiplos PDFs:
    - Comparar texto na mesma posição entre documentos
    - Texto idêntico em todos → label (ex: "Cliente:")
    - Texto diferente na mesma posição → valor dinâmico (ex: "João" / "Maria")
  - [x] Com 1 PDF:
    - Usar heurísticas: texto seguido de ":" provavelmente é label
    - Marcar confiança como "inferred" em vez de "confirmed"
    - Vision AI pode complementar inferência (Story 5.10)
  - [x] Registrar no StageRegistry

- [x] Task 3 — Implementar Stability Classification (AC: 3)
  - [x] Criar `backend/services/stages/stability_classification.py`
  - [x] Para cada elemento detectado, classificar:
    - `stable`: mesma posição E mesmo conteúdo em todos os documentos
    - `variable`: mesma posição, conteúdo diferente entre documentos
    - `absent`: presente em alguns documentos, ausente em outros
  - [x] Calcular score de estabilidade: % de documentos onde o elemento aparece
  - [x] Com 1 PDF: todos elementos classificados como "unknown" ou inferidos via heurística
  - [x] Registrar no StageRegistry

- [x] Task 4 — Implementar Variant Detection (AC: 4)
  - [x] Criar `backend/services/stages/variant_detection.py`
  - [x] Detectar campos opcionais: elementos `absent` em alguns docs → `optional_field`
  - [x] Detectar seções condicionais: grupo de campos adjacentes com mesmo padrão de presença → `conditional_section`
  - [x] Detectar tabelas dinâmicas: tabelas com contagem variável de linhas entre docs
  - [x] Output: variants[] com tipo (optional/conditional/dynamic_table), elementos afetados, padrão de presença
  - [x] Registrar no StageRegistry

- [x] Task 5 — Implementar Normalization (AC: 5)
  - [x] Criar `backend/services/stages/intelligence_normalization.py`
  - [x] Consolidar outputs dos estágios 8-11 em metadados estruturados
  - [x] Para cada LayoutType, produzir:
    - labels[]: lista de TextBlocks classificados como label
    - dynamic_fields[]: lista de TextBlocks classificados como valor dinâmico
    - stability_map: mapa elemento → classificação de estabilidade
    - variants[]: lista de variantes detectadas
    - conditional_sections[]: grupos condicionais
  - [x] Registrar no StageRegistry

- [x] Task 6 — Implementar flag de documento único (AC: 8)
  - [x] Quando apenas 1 PDF: adicionar metadata `{ single_document: true, reduced_accuracy: true }`
  - [x] Ajustar mensagens SSE para indicar confiança reduzida
  - [x] Labels inferidos (não confirmados) marcados com `confidence: "inferred"`

- [x] Task 7 — Escrever testes (AC: 9)
  - [x] Teste label vs valor: "Cliente:" sempre igual → label, "João"/"Maria" → dinâmico
  - [x] Teste estabilidade: elemento presente em todos → stable, ausente em 1 → absent
  - [x] Teste variantes: campos opcionais, seções condicionais, tabelas dinâmicas
  - [x] Teste fallback 1 PDF: executa com confiança reduzida, flag ativa

## Dev Notes

### Multi-Example Analysis — DIFERENCIAL CHAVE
- Esta é a funcionalidade que diferencia o sistema de ferramentas simples
- Com 3-5 PDFs, a precisão aumenta significativamente:
  - "Cliente: João" vs "Cliente: Maria" → "Cliente:" = label, valor = dinâmico
  - Com 1 PDF, não há como confirmar — usa heurísticas

### Heurísticas para 1 PDF
- Texto terminando em `:` → provavelmente label
- Texto em negrito/maior → provavelmente título ou label
- Texto numérico ou com formato (CPF, data, moeda) → provavelmente valor
- Texto curto adjacente a texto formatado → provavelmente label

### Seções Condicionais
- Campos adjacentes com mesmo padrão de presença formam um grupo
- Exemplo: "Avalista", "CPF Avalista", "Endereço Avalista" — todos presentes ou todos ausentes
- Gera `<!-- ko if: avalista -->` no template Knockout

### Classificação de Estabilidade → Confidence Scoring
- Dados de estabilidade alimentam o cálculo de confiança na Story 5.11
- Elementos `stable` → alta confiança no mapeamento
- Elementos `absent` → necessitam atenção do operador

### Variant Detection → MultiDoc Analyzer UI
- Variantes detectadas alimentam a UI do MultiDoc Analyzer (Epic 9)
- Operador pode revisar e confirmar variantes no editor

### FR Referenciado
- FR37: Inteligência de layout por análise multi-documento

### Testing
- pytest para testes backend
- Test files: `backend/tests/test_*.py`

## File List
- `backend/services/stages/layout_alignment.py` — Stage 12: Layout Alignment
- `backend/services/stages/multi_example_analysis.py` — Stage 13: Multi-Example Analysis
- `backend/services/stages/stability_classification.py` — Stage 14: Stability Classification
- `backend/services/stages/variant_detection.py` — Stage 15: Variant Detection
- `backend/services/stages/intelligence_normalization.py` — Stage 16: Intelligence Normalization
- `backend/services/stages/register_all.py` — adicionado register_bloco4()
- `backend/tests/test_layout_intelligence.py` — 19 testes pytest

## Completion Notes
- 112 testes passando (93 anteriores + 19 novos)
- Todos os 5 estágios (12-16) implementados e registrados no StageRegistry
- Stage 12 (Layout Alignment): computa offsets de coordenadas por cluster; trivial com 1 PDF
- Stage 13 (Multi-Example): compara texto por posição (bbox overlap ≥80%); heurísticas colon/numeric para 1 PDF
- Stage 14 (Stability): classifica stable/variable/absent + stability_score; unknown para 1 PDF
- Stage 15 (Variant Detection): optional_field, conditional_section (adjacência Y ≤30pt), dynamic_table
- Stage 16 (Normalization): consolida em intelligence{} dict por cluster_id + re-serializa layout_types
- Flag single_document/reduced_accuracy propagada via context["intelligence_metadata"]
- SSE emitido por todos os 5 estágios

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
| 2026-03-16 | 1.1 | Ajustes PO: depends_on, fix Testing | River (@sm) |
| 2026-03-17 | 1.2 | Implementação completa — 5 stages + 19 testes | Dex (@dev) |
| 2026-03-17 | 1.3 | fix: declarar _intelligence como campo opcional no dataclass LayoutType | Dex (@dev) |

## QA Results

**Reviewer:** Quinn (Test Architect)
**Date:** 2026-03-17
**Gate:** PASS

### Review Checklist
- Code Quality: ✓ Código bem estruturado, docstrings completas, type hints em todas as funções, exceções do emit_progress capturadas com bare-except intencional (BLE001 suprimido)
- Test Coverage: ✓ 19 testes cobrem todos os 5 estágios: label vs dinâmico, estabilidade stable/variable/absent/unknown, variantes optional/conditional/dynamic_table, fallback 1 PDF, SSE, end-to-end e registro no StageRegistry
- AC Compliance: ✓ Todos os 9 ACs cobertos — Stages 12-16 implementados, SSE emitido, flag single_document/reduced_accuracy ativa, output intelligence{} estruturado por cluster_id
- Security: ✓ Sem input externo não validado; context dict é passagem interna de pipeline; sem SQL, sem filesystem writes, sem serialização de credenciais
- Performance: ~ doc_count aproximado via len(samples) em Stage 14 (heurística aceitável para o domínio); sem otimização de lookup de bbox em Stage 13 para grandes volumes (O(n²) por cluster — aceitável para PDFs típicos)

### Issues Found
- **ISSUE-1** [medium] [suggested_owner: dev] Stage 16 anota `lt._intelligence` como atributo dinâmico em objeto LayoutType (linha 132: `lt._intelligence = intel`), padrão já identificado em Story 5.7 como violação de contrato de dataclass. Risco de AttributeError fora do contexto do pipeline ou em serialização futura.
- **ISSUE-2** [low] [suggested_owner: dev] Numeração dos estágios nos ACs (AC 1-5 citam "Stage 8-12") não corresponde à implementação real (Stages 12-16). Inconsistência herdada da story draft — sem impacto funcional mas pode confundir manutenção futura.
- **ISSUE-3** [low] [suggested_owner: dev] Heurística de 1 PDF em `_analyse_cluster_single_pdf`: strings com até 30 caracteres são classificadas como `label` por default. Pode sobre-classificar frases curtas como labels — aceitável dada a nota "confidence: inferred".
- **ISSUE-4** [low] [suggested_owner: dev] Teste `test_register_bloco4_wires_stages` hardcoda `pipeline.total_stages == 28` — fragilidade já presente em 5.7, mesma recomendação: tornar a assertion dinâmica.

### Gate Status
Gate: PASS → docs/qa/gates/5.8-layout-intelligence.yml
Implementação completa e correta: todos os 9 ACs cobertos, 19/19 testes passando (189 no suite total), SSE emitido por todos os 5 estágios, flag single-document propagada corretamente. Issues encontrados são de severidade média/baixa e não bloqueiam a funcionalidade.
