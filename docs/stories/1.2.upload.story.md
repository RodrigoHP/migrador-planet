---
epic: 1
story: 2
title: "Step 1 — Upload"
status: ReadyForReview
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [api_contract_validation, sse_integration_test, file_api_test, error_handling_review]
---

# Story 1.2: Step 1 — Upload

## Status

ReadyForReview

## Story

**As a** Operador Técnico,
**I want** uma tela de upload onde posso selecionar PDF, XSD e arquivo de dados, ver a cross-validation automática e acompanhar o progresso da análise via SSE,
**so that** meus arquivos sejam enviados ao backend e eu receba o resultado da extração de campos sem precisar sair da tela.

## Acceptance Criteria

1. Rota `/upload` renderiza `UploadPage` com `WizardLayout` e `WizardStepper` indicando step 1 ativo
2. Três `FileDropzone` independentes para PDF, XSD e Dados (JSON/XML) usando `showOpenFilePicker()` da File System Access API — fallback para `<input type="file">` quando API não disponível
3. Após seleção dos 3 arquivos, `CrossValidationBadge` aparece com status `ok` ou `divergence` com lista de divergências
4. Botão "Analisar" habilitado somente após 3 arquivos selecionados; desabilitado durante processamento
5. Ao clicar "Analisar": `POST /upload/pdf`, `POST /upload/xsd`, `POST /upload/data` em sequência, depois `POST /extract`; `session.jobId` preenchido com o jobId retornado
6. `useSSE(session.jobId)` conecta ao `GET /progress/{jobId}`; `ProgressBar` e `ProgressLabel` atualizam em tempo real com `processingPct` e `processingStep`
7. Após extração concluída: `session.extraction` populado com `ExtractionResult`, router navega para `/campos`
8. Erro fatal (PDF inválido, 500 backend): alert vermelho com mensagem de erro, botão "Tentar novamente" redefine o estado para inicial
9. Erro recuperável (XSD sem campos obrigatórios): quando backend retornar warning de validação de XSD (HTTP 200/202 com payload de warning), banner amarelo visível e fluxo pode continuar
10. `session store` persistido corretamente em memória (Pinia) após extração bem-sucedida, mantendo `jobId`, progresso e `extraction` durante a sessão atual (sem persistência em disco nesta story)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Task 1 — Criar `UploadPage.vue` com layout (AC: 1)
  - [x] Criar `src/organisms/WizardStepper.vue` com prop `currentStep: 0|1|2|3|4|5` e step 1 ativo em `/upload`
  - [x] Criar `src/templates/WizardLayout.vue` usando `AppHeader` e slot para conteúdo da página
  - [x] Usar `WizardLayout` template com `WizardStepper` (step 1 ativo)
  - [x] Registrar rota `/upload` no router

- [x] Task 2 — Criar Atom `Button.vue` (AC: 4)
  - [x] Props: `variant: 'primary'|'secondary'|'ghost'|'danger'`, `size: 'sm'|'md'|'lg'`, `loading: boolean`, `disabled: boolean`
  - [x] Classes Tailwind conforme front-end-spec seção 6.4
  - [x] Slot default para texto

- [x] Task 3 — Criar Molecule `FileDropzone.vue` (AC: 2)
  - [x] Props: `accept: string`, `label: string`, `modelValue: File | null`
  - [x] Emite `update:modelValue` ao selecionar arquivo
  - [x] Usa `showOpenFilePicker()` com filtro por tipo; fallback para `<input type="file">`
  - [x] Exibe nome do arquivo e tamanho após seleção
  - [x] Drag-and-drop support com classe visual de hover

- [x] Task 4 — Implementar `useFileSystem.ts` (AC: 2)
  - [x] `pickPDF()`: `showOpenFilePicker({ types: [{ accept: { 'application/pdf': ['.pdf'] } }] })`
  - [x] `pickXSD()`: `showOpenFilePicker({ types: [{ accept: { 'text/xml': ['.xsd'] } }] })`
  - [x] `pickData()`: `showOpenFilePicker({ types: [{ accept: { 'application/json': ['.json'], 'text/xml': ['.xml'] } }] })`
  - [x] Retorna `ArrayBuffer` de cada arquivo
  - [x] Detecta suporte a File System Access API (`'showOpenFilePicker' in window`)

- [x] Task 5 — Criar Atom `CrossValidationBadge` (AC: 3)
  - [x] Exibe após 3 arquivos selecionados
  - [x] Status `ok`: Badge verde "Arquivos compatíveis"
  - [x] Status `divergence`: Badge amarelo + lista de divergências

- [x] Task 6 — Implementar chamadas API e jobId (AC: 5)
  - [x] `POST /upload/pdf` com FormData (arquivo como blob)
  - [x] `POST /upload/xsd` idem
  - [x] `POST /upload/data` idem
  - [x] Contrato de sequência: chamar `/extract` apenas após sucesso dos 3 uploads (`res.ok` em cada etapa)
  - [x] Contrato de erro: `POST /upload/pdf` e `POST /extract` com HTTP 4xx/5xx => erro fatal (bloqueia fluxo)
  - [x] Contrato recuperável: warning de XSD (HTTP 200/202 + payload com warning) => exibir banner e permitir continuar
  - [x] `POST /extract` → recebe `{ jobId: string }`
  - [x] Preenche `session.jobId`

- [x] Task 7 — Implementar `useSSE.ts` e ProgressBar/Label (AC: 6)
  - [x] `useSSE(jobId)`: cria `EventSource(${API_BASE}/progress/${jobId.value})`
  - [x] Ouve evento `message`: atualiza `session.processingStep` e `session.processingPct`
  - [x] Fecha EventSource em `onUnmounted` ou quando `event.data.status === 'done'`
  - [x] Atom `ProgressBar.vue`: prop `value: number`, `animated: boolean`, `transition: width 300ms linear`
  - [x] Atom `ProgressLabel.vue`: Spinner + texto dinâmico de `session.processingStep`

- [x] Task 8 — Navegar para /campos após extração (AC: 7)
  - [x] Ao receber evento SSE com status `done`, popular `session.extraction`
  - [x] `router.push('/campos')` — guard já verifica `session.extraction`

- [x] Task 9 — Tratamento de erros (AC: 8, 9)
  - [x] Erro fatal (status 4xx/5xx): `session.error` preenchido, alert vermelho renderizado
  - [x] Botão "Tentar novamente": reseta `session.error`, `session.isProcessing`, `session.jobId`
  - [x] Warning recuperável: banner amarelo, sem bloquear fluxo

## Dev Notes

### API Endpoints
[Source: docs/front-end-spec.md#12-integracao-backend]
```typescript
const API_BASE = import.meta.env.VITE_API_URL ?? ''

POST ${API_BASE}/upload/pdf   // FormData com arquivo PDF
POST ${API_BASE}/upload/xsd   // FormData com arquivo XSD
POST ${API_BASE}/upload/data  // FormData com arquivo JSON/XML
POST ${API_BASE}/extract      // → { jobId: string }
GET  ${API_BASE}/progress/{jobId}  // SSE stream
```

### Session Store — campos relevantes
[Source: docs/front-end-spec.md#9-stores-pinia-e-composables]
```typescript
// useSessionStore
currentStep: 0 | 1 | 2 | 3 | 4 | 5  // definir como 1 ao entrar em /upload
jobId: string | null
isProcessing: boolean
processingStep: string
processingPct: number
error: string | null
pdfFile: { name, pages, sizeKB, bytes: ArrayBuffer } | null
xsdFile: { name, fieldCount, optionalCount, bytes } | null
dataFile: { name, fieldCount, bytes } | null
crossValidation: { status: 'ok' | 'divergence' | null, divergences: [] }
extraction: ExtractionResult | null
```

### File System Access API
[Source: docs/front-end-spec.md#3-fluxos-de-usuario]
```typescript
// Detecção de suporte
const hasFileSystemAccess = 'showOpenFilePicker' in window

// Fallback para input type="file"
if (!hasFileSystemAccess) {
  const input = document.createElement('input')
  input.type = 'file'
  // ...
}
```

### Componentes já existentes da Story 1.1
- `AppHeader` organism
- `AppToast` organism (para errors)

### Componentes a criar nesta Story 1.2
- `WizardLayout` template (usa `AppHeader`)
- `WizardStepper` organism (recebe `currentStep` prop)

### SSE — useSSE composable
[Source: docs/front-end-spec.md#9-stores-pinia-e-composables]
```typescript
// Estrutura esperada dos eventos SSE:
// { step: string, pct: number, status: 'processing' | 'done' | 'error' }
// EventSource fecha automaticamente no onUnmounted
```

### Erros fatais vs recuperáveis
[Source: docs/front-end-spec.md#2-arquitetura-de-informacao]
- Fatal: PDF inválido, falha 500 backend → bloqueia, exige retry
- Recuperável: XSD sem campos obrigatórios → warning, não bloqueia

### Contrato mínimo para implementação (Story 1.2)
- `POST /upload/pdf`, `POST /upload/xsd`, `POST /upload/data` devem ser executados em sequência e validados individualmente.
- `POST /extract` só deve ser chamado quando os 3 uploads anteriores retornarem sucesso.
- Erro fatal: qualquer 4xx/5xx em `/upload/pdf` ou `/extract` => preencher `session.error` e bloquear até retry.
- Erro recuperável: warning de XSD retornado em payload de sucesso (200/202) => mostrar banner amarelo e seguir para extração.
- Critério de AC10: validação de persistência limitada ao estado em memória do Pinia durante a sessão atual.

### Testing
Verificar manualmente em Chrome/Edge:
- Seleção de arquivos via File System Access API
- SSE recebe eventos corretamente (testar com backend local)
- Progress bar atualiza em tempo real
- Navegação para /campos após conclusão

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Story criada | @sm (River) |
| 2026-03-11 | 1.1 | Story validada e aprovada para desenvolvimento | @po (Pax) |
| 2026-03-11 | 1.2 | Implementacao completa do step Upload (UI, upload sequencial, SSE, progresso e tratamento de erros) | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
GPT-5 Codex (Codex CLI)

### Completion Notes
- Implementados `WizardLayout` e `WizardStepper` e integrados na `UploadPage`.
- Criados atoms `Button`, `CrossValidationBadge`, `ProgressBar`, `ProgressLabel` e molecule `FileDropzone`.
- `UploadPage` implementada com:
  - selecao de 3 arquivos (PDF/XSD/Dados) com drag-and-drop e picker;
  - cross-validation local com status `ok`/`divergence`;
  - upload sequencial `/upload/pdf` → `/upload/xsd` → `/upload/data` → `/extract`;
  - captura de `jobId`, conexao SSE e atualizacao de progresso em tempo real;
  - navegacao para `/campos` ao concluir (`done`);
  - tratamento de erros fatais e warning recuperavel com reset via "Tentar novamente".
- `useSSE` estendido com callbacks `onDone`, `onError`, `onWarning` para orquestrar fluxo da pagina.
- Quality gates executados em `frontend`: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.

### File List
- frontend/src/App.vue
- frontend/src/atoms/index.ts
- frontend/src/atoms/Button.vue
- frontend/src/atoms/CrossValidationBadge.vue
- frontend/src/atoms/ProgressBar.vue
- frontend/src/atoms/ProgressLabel.vue
- frontend/src/molecules/index.ts
- frontend/src/molecules/FileDropzone.vue
- frontend/src/organisms/index.ts
- frontend/src/organisms/WizardStepper.vue
- frontend/src/templates/index.ts
- frontend/src/templates/WizardLayout.vue
- frontend/src/composables/useSSE.ts
- frontend/src/pages/UploadPage.vue

## QA Results
### Review Date
2026-03-11

### Reviewer
@qa (Quinn)

### Gate Decision
PASS

### Evidence Executed
- `npm run lint` (frontend): PASS
- `npm run typecheck` (frontend): PASS
- `npm test` (frontend): PASS (script informativo)
- `npm run build` (frontend): PASS

### Findings
1. Revisao anterior com `CONCERNS` foi enderecada:
   - `useSSE` agora propaga erro de conexao via callback `onError` e encerra o fluxo com feedback na UI.
   - `UploadPage` nao navega para `/campos` quando o evento `done` nao traz extracao valida.
2. Nenhum bug/regressao bloqueante adicional encontrado nesta rodada.

### Residual Risks
- Ainda sem suite automatizada de comportamento (unit/component/e2e) para cenarios de SSE intermitente/offline; validacao atual depende de build/typecheck + testes manuais.
