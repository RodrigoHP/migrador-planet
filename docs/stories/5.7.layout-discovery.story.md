---
epic: 5
story: 7
title: "Bloco 2: Layout Discovery — Skeletons, Clustering, Fingerprints e Registry"
status: InProgress
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [static_analysis, api_test]
depends_on: [5.5]
---

# Story 5.7: Bloco 2: Layout Discovery — Skeletons, Clustering, Fingerprints e Registry

## Status
InProgress

## Story
**As a** bloco do pipeline,
**I want** construir skeletons de layout, clusterizar páginas por similaridade, selecionar representantes, gerar fingerprints e verificar o registry de layouts,
**so that** o sistema identifique Layout Types e possa reutilizar templates conhecidos.

## Acceptance Criteria
1. Stage 3 — Skeleton Builder: criar LayoutSkeleton por página (text blocks + bounding boxes, candidatos a tabela, zonas de layout, grid info)
2. Stage 4 — Page Clustering: KMeans sobre feature vectors (num_blocks, avg_font_size, table_count, text_density, header_height_ratio) → clusters de layout
3. Stage 5 — Representative Selection: selecionar melhor página representante por cluster (de PDFs diferentes quando múltiplos)
4. Stage 6 — Fingerprint Generation: gerar assinatura estrutural { tableCount, columnCount, headerBlocks, bodyZoneRatio, footerPresent } + hash
5. Stage 7 — Registry Lookup: consultar layout_registry no Supabase; se match → retornar template_id existente (pular para mapping); se sem match → continuar
6. Todos os 5 estágios registrados no orchestrator, emitindo SSE progress
7. Output: LayoutTypes[] com { cluster_id, name, representative_page, fingerprint, page_count, pages[] }
8. Para PDFs grandes (500+ páginas), processar apenas ~5-10 representantes por cluster
9. Se Supabase layout_registry estiver inacessível, Registry Lookup é ignorado (skip) com warning SSE "Registry indisponível — layout tratado como novo"; pipeline continua normalmente
10. Testes cobrem: precisão do clustering, geração de fingerprint, registry lookup hit/miss, registry offline

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [ ] Task 1 — Implementar Skeleton Builder (AC: 1)
  - [ ] Criar `backend/services/stages/skeleton_builder.py`
  - [ ] Para cada página de cada PDF, construir LayoutSkeleton:
    - text_blocks com bounding boxes (do ParsedDocument)
    - table_candidates: regiões com grid info detectado
    - layout_zones: header (top 15%), body (middle), footer (bottom 10%)
    - grid_info do estágio anterior
  - [ ] Modelo `LayoutSkeleton`: page_ref, text_blocks[], table_candidates[], zones{}, grid_info
  - [ ] Registrar no StageRegistry

- [ ] Task 2 — Implementar Page Clustering (AC: 2, 8)
  - [ ] Criar `backend/services/stages/page_clustering.py`
  - [ ] Construir feature vector por página:
    - num_blocks: quantidade de blocos de texto
    - avg_font_size: tamanho médio de fonte
    - table_count: número de candidatos a tabela
    - text_density: texto total / área da página
    - header_height_ratio: altura da zona header / altura da página
  - [ ] Usar scikit-learn KMeans com n_clusters auto-determinado (silhouette score)
  - [ ] Para PDFs grandes (500+ páginas): amostrar ~50 páginas, clusterizar, atribuir restantes
  - [ ] Registrar no StageRegistry

- [ ] Task 3 — Implementar Representative Selection (AC: 3)
  - [ ] Criar `backend/services/stages/representative_selection.py`
  - [ ] Para cada cluster, selecionar a página mais próxima do centroid
  - [ ] Preferir páginas de PDFs diferentes (quando múltiplos PDFs)
  - [ ] Selecionar 1 representante principal + até 2 secundários por cluster
  - [ ] Registrar no StageRegistry

- [ ] Task 4 — Implementar Fingerprint Generation (AC: 4)
  - [ ] Criar `backend/services/stages/fingerprint_generation.py`
  - [ ] Para cada cluster/LayoutType, gerar fingerprint:
    - tableCount: número de tabelas
    - columnCount: número de colunas do grid
    - headerBlocks: quantidade de blocos no header
    - bodyZoneRatio: proporção da zona body
    - footerPresent: boolean
  - [ ] Gerar hash SHA-256 do fingerprint para lookup rápido
  - [ ] Registrar no StageRegistry

- [ ] Task 5 — Implementar Registry Lookup (AC: 5)
  - [ ] Criar `backend/services/stages/registry_lookup.py`
  - [ ] Consultar tabela `layout_registry` no Supabase por fingerprint_hash
  - [ ] Se match encontrado: retornar template_id existente, marcar Layout Type como "reusable"
  - [ ] Se sem match: marcar Layout Type como "new", continuar pipeline normalmente
  - [ ] Registrar no StageRegistry

- [ ] Task 6 — Definir modelo LayoutType (AC: 7)
  - [ ] Criar/atualizar `backend/models/layout_type.py`
  - [ ] Modelo `LayoutType`: cluster_id, name, representative_page, fingerprint, fingerprint_hash, page_count, pages[], is_reusable, template_id (nullable)
  - [ ] Nome auto-gerado baseado em características: "Capa", "Transações", "Resumo", etc.

- [ ] Task 7 — Escrever testes (AC: 9)
  - [ ] Teste de clustering: páginas similares agrupadas corretamente
  - [ ] Teste de fingerprint: mesmo layout → mesmo hash
  - [ ] Teste de registry lookup hit: fingerprint existente retorna template_id
  - [ ] Teste de registry lookup miss: fingerprint novo continua pipeline
  - [ ] Teste com 1 PDF (poucas páginas): provável 1 Layout Type

## Dev Notes

### Bibliotecas
- scikit-learn para KMeans clustering
- numpy para feature vectors
- hashlib para SHA-256 de fingerprints

### Supabase layout_registry
```sql
CREATE TABLE layout_registry (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  fingerprint_hash TEXT UNIQUE NOT NULL,
  fingerprint JSONB NOT NULL,
  template_id UUID REFERENCES templates(id),
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### Nomes de Layout Type
- Auto-gerados com base nas características detectadas:
  - Muitas tabelas → "Transações" ou "Extrato"
  - Poucos blocos grandes → "Capa" ou "Resumo"
  - Grid complexo → "Detalhamento"
  - Footers proeminentes → "Rodapé Legal"

### Cenários de PDFs
- 1 PDF com poucas páginas → provavelmente 1 Layout Type
- 1 PDF com muitas páginas → múltiplos Layout Types (capa, corpo, resumo)
- Múltiplos PDFs → melhor clustering por ter mais data points de variação

### Otimização para PDFs Grandes
- PDFs com 500+ páginas: amostrar ~50 páginas para clustering
- Atribuir restantes ao cluster mais próximo sem recalcular
- Selecionar ~5-10 representantes por cluster para análise detalhada

### FR Referenciado
- FR37: Detecção automática de Layout Types por agrupamento de páginas

### Testing
- pytest para testes backend
- Test files: `backend/tests/test_*.py`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
| 2026-03-16 | 1.1 | Ajustes PO: depends_on, AC fallback Supabase offline, fix Testing | River (@sm) |
