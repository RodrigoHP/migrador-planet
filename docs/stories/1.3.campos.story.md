---
epic: 1
story: 3
title: "Step 2 — Campos"
status: ReadyForReview
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [pdfjs_integration_test, mapping_store_validation, ui_state_test]
---

# Story 1.3: Step 2 — Campos

## Status

ReadyForReview

## Story

**As a** Operador Técnico,
**I want** revisar o mapeamento de campos gerado pela IA em uma tela com três painéis — PDF viewer, tabela de campos e painel de detalhes — podendo corrigir manualmente campos com confiança baixa ou não encontrados,
**so that** o mapeamento final reflita com precisão a correspondência entre campos XSD e texto do PDF antes de avançar para o layout.

## Acceptance Criteria

1. Rota `/campos` renderiza `CamposPage` com layout de 3 painéis (PDF 50% | Tabela 30% | Detalhes 20%)
2. `PDFViewer` organism exibe o PDF carregado usando PDF.js (lazy import), com navegação de páginas
3. Ao selecionar um campo na tabela, `PDFViewer` destaca o bounding box do campo na página correspondente (`pageRef`)
4. `FieldMappingTable` exibe todos os campos com colunas: Campo XSD, Texto PDF, Tipo, Confiança, Status
5. Cada campo exibe `FieldStatusBadge` (ok/ambiguous/not_found/optional) e `ConfidenceBadge` (high/medium/low)
6. Clicar em um campo abre `FieldDetailPanel` com detalhes, candidatos alternativos e inline editor para `jsonPath`
7. Campo editado manualmente exibe `ManualEditIndicator` (ícone diferenciador); `isManual: true` no store
8. Botão "Confirmar Mapeamento" desabilitado se existir qualquer campo required com status `not_found`
9. Ao confirmar: `mappingStore.confirmed = true`, router navega para `/layout`
10. `SplitPaneLayout` com largura mínima de 280px por painel

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Task 1 — Criar `CamposPage.vue` com `SplitPaneLayout` 3 painéis (AC: 1, 10)
  - [x] `SplitPaneLayout` template: 3 colunas com largura mínima 280px cada
  - [x] Registrar rota `/campos` com guard: `session.extraction !== null`

- [x] Task 2 — Criar `PDFViewer` organism (AC: 2, 3)
  - [x] Lazy import: `const pdfjs = await import('pdfjs-dist')`
  - [x] Renderiza página atual do PDF em `<canvas>`
  - [x] Prop `boundingBox: {x, y, width, height} | null` — desenha overlay ao mudar
  - [x] Controles de paginação (página anterior/próxima, número atual/total)
  - [x] Atualiza página ao receber `pageRef` do campo selecionado

- [x] Task 3 — Criar Atoms `FieldStatusBadge` e `ConfidenceBadge` (AC: 5)
  - [x] `FieldStatusBadge`: `ok` verde, `ambiguous` amarelo, `not_found` vermelho, `optional` cinza
  - [x] `ConfidenceBadge`: `high` verde, `medium` amarelo, `low` vermelho

- [x] Task 4 — Criar `FieldMappingTable` organism (AC: 4, 5)
  - [x] Recebe `fields: FieldMapping[]` do `useMappingStore`
  - [x] Colunas: Campo XSD, Texto PDF, Tipo, Confiança, Status
  - [x] Row clicável: emite `select(fieldId)`
  - [x] Row selecionada destacada com `bg-primary-50`

- [x] Task 5 — Criar Atom `ManualEditIndicator` (AC: 7)
  - [x] Ícone `lucide-vue-next` (ex: `PenLine`) com Tooltip "Editado manualmente"
  - [x] Visível apenas quando `field.isManual === true`

- [x] Task 6 — Criar `FieldDetailPanel` organism (AC: 6, 7)
  - [x] Exibe detalhes do campo selecionado: id, pdfText, jsonPath, tipo, status
  - [x] Lista de candidatos alternativos clicáveis (`field.candidates`)
  - [x] Inline editor para `jsonPath`: Input com botão "Salvar"
  - [x] Ao salvar: `mappingStore.updateField({ id, jsonPath, isManual: true, status: 'ok' })`

- [x] Task 7 — Implementar lógica de confirmação (AC: 8, 9)
  - [x] Computed `hasUnresolvedRequired`: `fields.filter(f => !f.optional && f.status === 'not_found').length > 0`
  - [x] Botão "Confirmar Mapeamento" com `:disabled="hasUnresolvedRequired"`
  - [x] Ao clicar: `mappingStore.confirmed = true`, `router.push('/layout')`

- [x] Task 8 — Popular mapping store com dados da extração (AC: 4)
  - [x] No `onMounted` do `CamposPage`: ler `session.extraction.fields` e popular `mappingStore.fields`
  - [x] Se `mappingStore.fields` já populado (retomar projeto): não sobrescrever

## Dev Notes

### Mapping Store — Interface FieldMapping
[Source: docs/front-end-spec.md#9-stores-pinia-e-composables]
```typescript
interface FieldMapping {
  id: string
  pdfText: string
  jsonPath: string
  type: 'text' | 'date' | 'currency' | 'list' | 'composite'
  confidence: 'high' | 'medium' | 'low'
  status: 'ok' | 'ambiguous' | 'not_found' | 'optional'
  candidates?: string[]
  isManual: boolean
  pageRef?: number
  boundingBox?: { x: number; y: number; width: number; height: number }
}
```

### PDF.js Lazy Import
[Source: docs/front-end-spec.md#11-performance]
```typescript
// NUNCA importar no topo do arquivo — deve ser lazy
const pdfjs = await import('pdfjs-dist')
pdfjs.GlobalWorkerOptions.workerSrc = '/pdf.worker.min.js'
```
Copiar `pdfjs-dist/build/pdf.worker.min.js` para `public/` ou configurar via Vite asset.

### Layout 3 painéis
[Source: docs/front-end-spec.md#4-especificacao-de-telas]
- Esquerda: PDF.js preview (50%)
- Centro: tabela de campos (30%)
- Direita: painel de detalhes do campo selecionado (20%)
- Largura mínima por painel: 280px

### Dependência de Story 1.2
- `session.extraction.fields` deve estar populado antes desta tela
- Guard de rota garante isso (ver Story 1.1 — router)

### Testing
- Testar seleção de campo e highlight de bounding box no PDF
- Testar edição manual de campo e verificação de `isManual: true` no store
- Testar botão desabilitado com campos `not_found`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Story criada | @sm (River) |
| 2026-03-11 | 1.1 | Story validada e aprovada para desenvolvimento | @po (Pax) |
| 2026-03-11 | 1.2 | Implementacao do step Campos (split pane, PDFViewer, tabela, painel de detalhe e confirmacao) | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
GPT-5 Codex (Codex CLI)

### Completion Notes
- Implementado `SplitPaneLayout` com 3 painéis e largura mínima de 280px.
- `CamposPage` integrada com `WizardLayout` + `WizardStepper` (step 2).
- Implementado `PDFViewer` com lazy import de PDF.js, paginação, e highlight de `boundingBox` por `pageRef`.
- Criados atoms: `FieldStatusBadge`, `ConfidenceBadge`, `ManualEditIndicator`.
- Criados organisms: `FieldMappingTable`, `FieldDetailPanel`.
- Mapeamento é carregado de `session.extraction.fields` no `onMounted` sem sobrescrever estado já existente.
- Edição manual no detalhe salva em store com `isManual: true` e `status: 'ok'`.
- Confirmação bloqueia com `not_found`, e ao confirmar navega para `/layout`.
- Quality gates executados em `frontend`: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.

### File List
- frontend/src/atoms/index.ts
- frontend/src/atoms/FieldStatusBadge.vue
- frontend/src/atoms/ConfidenceBadge.vue
- frontend/src/atoms/ManualEditIndicator.vue
- frontend/src/templates/index.ts
- frontend/src/templates/SplitPaneLayout.vue
- frontend/src/organisms/index.ts
- frontend/src/organisms/PDFViewer.vue
- frontend/src/organisms/FieldMappingTable.vue
- frontend/src/organisms/FieldDetailPanel.vue
- frontend/src/pages/CamposPage.vue
- docs/stories/1.3.campos.story.md

## QA Results
### Review Date
2026-03-11

### Reviewer
@qa (Quinn)

### Gate Decision
PASS

### Evidence Executed
- `npm run lint` (frontend): PASS
- `npm run typecheck` (frontend): PASS
- `npm test` (frontend): PASS (script informativo)
- `npm run build` (frontend): PASS

### Findings
- Nenhum bug/regressao bloqueante identificado na implementacao da 1.3.
- Fluxos principais cobertos na revisao:
  - layout de 3 painéis com minimo de 280px;
  - tabela + seleção + painel de detalhes + edição manual;
  - bloqueio de confirmação em campos `not_found`;
  - navegação para `/layout` ao confirmar;
  - `PDFViewer` com lazy import e paginação.

### Residual Risks
- Nao ha suite automatizada de comportamento (unit/component/e2e) para cenarios de PDF.js/Canvas e interações de tabela; validacao permanece majoritariamente manual.
