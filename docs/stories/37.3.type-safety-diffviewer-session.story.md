# Story 37.3 — Corrigir type safety em DiffViewer, session.ts e coverageStore

- **ID:** 37.3
- **Title:** Corrigir type safety em DiffViewer, session.ts e coverageStore
- **Type:** tech-debt
- **Status:** Done
- **Priority:** P3
- **Epic:** 37
- **Complexity:** S

## Story

As a developer, I want unsafe type assertions (`ref<any>`, `as unknown as Record`) replaced with proper typed interfaces, so that TypeScript catches type errors at compile time instead of runtime.

## Acceptance Criteria

- [ ] `DiffViewer.vue`: `ref<any>(null)` para pdfDocA/pdfDocB substituido por tipo `PDFDocumentProxy | null` importado de pdfjs-dist
- [ ] `session.ts`: Pattern `as unknown as Record<string,unknown>` eliminado em `applyTableCellFlags()` e `reconcileFieldBindings()` — criar interface `NodeProperties` tipada
- [ ] `coverageStore.ts`: Cast `layoutStore as unknown as {...}` removido — usar getter tipado ou import correto
- [ ] `npm run typecheck` passa sem erros
- [ ] Zero ocorrencias de `as any` em codigo de producao (excluindo testes e declaracoes .d.ts)
- [ ] Testes existentes continuam passando sem alteracoes

## Tasks

- [x] Importar `PDFDocumentProxy` de pdfjs-dist no DiffViewer.vue e tipar refs
- [x] Estender TreeNode com `block_id?` e `suggested_binding?` em template.types.ts
- [x] Refatorar `applyTableCellFlags()` para usar campos tipados (eliminar double casts)
- [x] Refatorar `reconcileFieldBindings()` para usar campos tipados
- [x] Corrigir coverageStore.ts e confidenceStore.ts — acessar activeLayoutId diretamente
- [x] Rodar typecheck e corrigir eventuais cascatas
- [x] Validar todos os testes passam

## Scope

**IN:** Substituir type assertions inseguras por interfaces tipadas em 3 arquivos
**OUT:** Refatoracao funcional, mudancas de logica, novos testes, mudancas em stores nao listados

## Dev Notes

- **DiffViewer.vue:236** — `ref<any>(null)` — importar `PDFDocumentProxy` de `pdfjs-dist`
- **session.ts:20-32** — `as unknown as Record<string,unknown>` usado para acessar `block_id`, `overlay_type`, `is_header_row` em nodes do templateStore. Criar interface com esses campos opcionais
- **coverageStore.ts:40** — Cast para acessar `activeLayoutId` do layoutStore. O layoutStore ja exporta esse ref — verificar se o import esta correto
- Todas as mudancas sao refatoracao de tipos sem mudanca funcional
- Impacto zero em runtime — apenas melhoria de compile-time safety

## Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-04-08 | @sm | Story criada a partir de backlog QA tech-debt TD-3 |
| 2026-04-08 | @dev | Implemented: PDFDocumentProxy tipado, TreeNode estendido, 5 double-casts eliminados, typecheck OK |
