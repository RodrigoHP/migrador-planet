---
epic: 7
story: 8
title: "Format String Customizado + Tematização Condicional"
status: Done
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [static_analysis, ui_behavior_check]
depends_on: [7.4, 6.4, 6.1]
---

# Story 7.8: Format String Customizado + Tematização Condicional

## Status
Done

## Story
**As a** operador,
**I want** definir format strings customizados combinando múltiplos campos JSON e regras de aparência condicional,
**so that** o template possa exibir campos concatenados e variações de estilo baseadas em valores dos dados.

## Acceptance Criteria
1. Editor de format string: input de texto com autocomplete para campos (ex: `"{Logradouro}, {Numero} - {Bairro}"`) (FR21)
2. Autocomplete sugere campos XSD conforme operador digita `{`
3. Format string gera função computed em base.js
4. Preview: exibe saída formatada com dados de exemplo
5. Aparência condicional: no Inspector, seção "Estilo Condicional" com regras: SE [campo] [operador] [valor] ENTÃO [propriedade] = [valor] (FR30)
6. Propriedades suportadas para estilo condicional: cor do texto, cor de fundo, imagem, visibilidade
7. Múltiplas regras suportadas por elemento
8. Gerado como computed observables em base.js
9. Caracteres `{` e `}` literais são escapados com `\{` e `\}` no editor de format string
10. Campos não encontrados no XSD são destacados em vermelho no editor de format string com tooltip "Campo não encontrado no XSD"
11. `npm run build` passa sem erros

## CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [x] Implementar editor de format string com autocomplete (AC: #1, #2)
  - [x] Input de texto com detecção de `{` para trigger de autocomplete
  - [x] Autocomplete populado de mappingStore.allFields
  - [x] Suporte a múltiplos campos na mesma string (ex: `"{Logradouro}, {Numero} - {Bairro}"`)
- [x] Implementar geração de computed em base.js (AC: #3)
  - [x] Format string gera: `ko.computed(function() { return data.Logradouro() + ", " + data.Numero() + " - " + data.Bairro(); })`
  - [x] Parser de format string → código Knockout
- [x] Implementar preview de format string (AC: #4)
  - [x] Usar dataset de teste ativo ou dados sintéticos
  - [x] Exibir resultado formatado em tempo real
- [x] Implementar construtor de aparência condicional (AC: #5, #6, #7)
  - [x] Seção "Estilo Condicional" no Inspector
  - [x] Regra: SE [campo dropdown] [operador dropdown] [valor input] ENTÃO [propriedade dropdown] = [valor input/picker]
  - [x] Propriedades: cor do texto (color picker), cor de fundo (color picker), imagem (file picker), visibilidade (dropdown)
  - [x] Botão [+ Regra] para adicionar múltiplas regras por elemento
- [x] Implementar geração de computed observables para estilo (AC: #8)
  - [x] Estilo condicional gera: `data-bind="style: { color: campo() > 0 ? 'green' : 'red' }"`
  - [x] Múltiplas regras combinadas no mesmo data-bind
- [x] Implementar escape de caracteres literais (AC: #9)
  - [x] Parser reconhece `\{` e `\}` como literais
  - [x] Preview exibe `{` e `}` corretamente
- [x] Implementar validação de campos no format string (AC: #10)
  - [x] Verificar cada `{Campo}` contra mappingStore.allFields
  - [x] Destacar campo inválido em vermelho com tooltip
- [x] Verificar `npm run build` passa (AC: #11)

## Dev Notes
- FR21: format strings combinando múltiplos campos com autocomplete
- FR30: regras de aparência condicional vinculadas a campos JSON
- Format string gera: `ko.computed(function() { return data.Logradouro() + ", " + data.Numero() + " - " + data.Bairro(); })`
- Estilo condicional gera: `data-bind="style: { color: campo() > 0 ? 'green' : 'red' }"`
- Autocomplete: popular de mappingStore.allFields quando `{` é digitado
- Preview usa dataset de teste ativo ou dados sintéticos
- Stack: Vue 3 + TypeScript + Pinia + Tailwind CSS

### FRs Referenciados
- FR21: Format strings customizados combinando múltiplos campos com autocomplete
- FR30: Tematização condicional — regras de aparência vinculadas a campos JSON

### Testing
- Vitest for unit tests
- Test files alongside source: `*.spec.ts` or `*.test.ts`

## File List
- `frontend/src/utils/formatStringGenerator.ts` — utilitário: parseFormatFields, generateFormatComputed, generateStyleBindings, interface StyleRule
- `frontend/src/molecules/FormatStringEditor.vue` — editor de format string com autocomplete, highlight e preview
- `frontend/src/molecules/ConditionalStyleRule.vue` — linha de regra: campo/operador/valor → propriedade=valor
- `frontend/src/molecules/ConditionalStyleSection.vue` — lista de regras com botão [+ Regra]
- `frontend/src/organisms/inspectors/ElementInspector.vue` — adicionadas seções "Format String" e "Estilo Condicional"
- `frontend/src/utils/formatStringGenerator.spec.ts` — testes Vitest para utilitário de geração
- `frontend/src/molecules/FormatStringEditor.spec.ts` — testes Vitest para FormatStringEditor
- `frontend/src/molecules/ConditionalStyleSection.spec.ts` — testes Vitest para ConditionalStyleSection

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
| 2026-03-16 | 1.1 | Ajustes PO: depends_on, AC escape chars, AC validação campo XSD | River (@sm) |
