---
epic: 9
story: 11
title: "Image Inspector Avançado — Upload, Remoção e Gestão de Assets"
status: Draft
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [static_analysis, ui_behavior_check]
depends_on: [6.3, 7.4, 8.1, 8.6]
---

# Story 9.11: Image Inspector Avançado — Upload, Remoção e Gestão de Assets

## Status
Draft

## Story
**As a** operador,
**I want** funcionalidade avançada de gestão de imagens: upload com persistência no servidor, remoção completa (template + tree + assets), validação de formato/tamanho, preview antes de confirmar substituição e detecção de SVG inline,
**so that** eu possa gerenciar os assets visuais do projeto com confiança, sabendo que as alterações são persistidas e validadas.

## Scope Boundary (vs. Stories 6.7 e 7.4)

| Capacidade | 6.7 (Display) | 7.4 (Editing) | **9.11 (Avançado)** |
|------------|---------------|---------------|---------------------|
| Exibir dimensões, escala, alinhamento, fonte | Read-only labels | Inputs editáveis | -- (já coberto) |
| Botões Substituir / Download | Placeholder (desabilitados) | File picker local + download | **Upload para servidor + persistência em assets/** |
| Remover imagem | -- | Botão "Remover do template" genérico (nó) | **Remoção completa: nó + arquivo em assets/ + referências HTML** |
| Validação de arquivo (tipo, tamanho) | -- | -- | **Validação client-side antes do upload** |
| Preview antes de confirmar substituição | -- | -- | **Modal de preview com comparação antes/depois** |
| SVG inline (FR32) | -- | -- | **Detecção de SVG + opção incorporar como SVG inline** |
| Galeria de assets do projeto | -- | -- | **Painel listando todos os assets em assets/** |

> **Regra:** 9.11 NÃO reimplementa display de propriedades (6.7) nem inputs editáveis básicos (7.4). Esta story estende o ImageInspector com funcionalidades server-side, validação e gestão de assets.

## Acceptance Criteria
1. **Upload com persistência**: ao substituir uma imagem via file picker, o arquivo é enviado ao servidor via `POST /api/templates/{template_id}/assets` e armazenado na pasta `assets/` do pacote de template; a referência `<img src="assets/{nome}">` no HTML é atualizada automaticamente.
2. **Remoção completa**: botão "Remover" no ImageInspector remove: (a) o nó da StructureTree, (b) a referência `<img>` do template HTML, (c) o arquivo físico em `assets/` via `DELETE /api/templates/{template_id}/assets/{filename}`; confirmação obrigatória antes de executar.
3. **Validação client-side**: antes do upload, validar: tipo de arquivo (PNG, JPG, WEBP, SVG — outros rejeitados com mensagem), tamanho máximo (5 MB — configurável), dimensões mínimas (10x10 px); exibir mensagem de erro específica para cada violação.
4. **Preview de substituição**: ao selecionar nova imagem no file picker, exibir modal com preview lado a lado (imagem atual vs. nova) com dimensões de ambas; operador confirma ou cancela antes do upload.
5. **SVG inline (FR32)**: quando a imagem selecionada ou extraída é SVG, oferecer toggle "Incorporar como SVG inline" que insere o conteúdo SVG diretamente no `index.html` em vez de referência `<img>`.
6. **Galeria de assets**: seção colapsável no ImageInspector listando todos os arquivos em `assets/` do projeto atual, com thumbnail, nome e tamanho; clicar em um asset seleciona-o como substituição (sem novo upload).
7. `npm run build` passa sem erros.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks

- [ ] Task 1 — Implementar serviço de upload de assets (AC: 1)
  - [ ] Criar/estender `frontend/src/services/assetService.ts`
    - [ ] `uploadAsset(templateId: string, file: File): Promise<AssetResponse>` — POST multipart/form-data para `/api/templates/{template_id}/assets`
    - [ ] `deleteAsset(templateId: string, filename: string): Promise<void>` — DELETE `/api/templates/{template_id}/assets/{filename}`
    - [ ] `listAssets(templateId: string): Promise<AssetInfo[]>` — GET `/api/templates/{template_id}/assets`
  - [ ] Tipo `AssetResponse`: `{ filename: string, path: string, size: number, dimensions: { width: number, height: number } }`
  - [ ] Tipo `AssetInfo`: `{ filename: string, path: string, size: number, thumbnailUrl: string }`
  - [ ] Tratamento de erros HTTP com mensagens localizadas (PT-BR)

- [ ] Task 2 — Implementar validação client-side de imagens (AC: 3)
  - [ ] Criar `frontend/src/utils/imageValidation.ts`
    - [ ] `validateImageFile(file: File): Promise<ValidationResult>` — retorna `{ valid: boolean, errors: string[] }`
    - [ ] Validar MIME type: `image/png`, `image/jpeg`, `image/webp`, `image/svg+xml`
    - [ ] Validar tamanho: máximo 5 MB (constante `MAX_IMAGE_SIZE_BYTES = 5 * 1024 * 1024`)
    - [ ] Validar dimensões mínimas via `Image()` load: 10x10 px mínimo
    - [ ] Mensagens de erro em PT-BR: "Formato não suportado (use PNG, JPG, WEBP ou SVG)", "Arquivo excede 5 MB", "Dimensões mínimas: 10×10 px"

- [ ] Task 3 — Implementar modal de preview de substituição (AC: 4)
  - [ ] Criar `frontend/src/components/molecules/ImageReplacePreview.vue`
    - [ ] Props: `currentImageUrl: string`, `newImageFile: File`, `currentDimensions: Dimensions`, `newDimensions: Dimensions`
    - [ ] Layout: duas colunas — "Atual" (esquerda) e "Nova" (direita) com thumbnails
    - [ ] Exibir dimensões abaixo de cada imagem: "{W} × {H} px"
    - [ ] Botões: "Confirmar substituição" (primary) e "Cancelar" (secondary)
    - [ ] Emits: `confirm`, `cancel`

- [ ] Task 4 — Estender ImageInspector com upload funcional (AC: 1, 4)
  - [ ] Modificar `frontend/src/components/organisms/inspectors/ImageInspector.vue`
    - [ ] Botão "Substituir": abre file picker → valida (Task 2) → preview modal (Task 3) → upload (Task 1) → atualiza `templateStore` com novo path
    - [ ] Botão "Baixar": download via `assetService` (URL direta para asset no servidor)
    - [ ] Loading state durante upload (spinner + desabilitar botões)
    - [ ] Toast de sucesso/erro após upload

- [ ] Task 5 — Implementar remoção completa de imagem (AC: 2)
  - [ ] Modificar `frontend/src/components/organisms/inspectors/ImageInspector.vue`
    - [ ] Botão "Remover" com ícone de lixeira
    - [ ] Ao clicar: modal de confirmação "Remover imagem '{filename}'? Esta ação remove o arquivo do projeto."
    - [ ] Ao confirmar:
      1. Chamar `assetService.deleteAsset()` para remover arquivo do servidor
      2. Remover nó da StructureTree via `templateStore.removeNode(nodeId)`
      3. Remover tag `<img>` correspondente do HTML no templateStore
    - [ ] Tratamento de erro: se delete falhar no servidor, não remover do template local

- [ ] Task 6 — Implementar detecção e toggle SVG inline (AC: 5)
  - [ ] Modificar `frontend/src/components/organisms/inspectors/ImageInspector.vue`
    - [ ] Detectar quando imagem selecionada tem MIME `image/svg+xml`
    - [ ] Exibir toggle "Incorporar como SVG inline" (visível apenas para SVGs)
    - [ ] Quando ativo: ler conteúdo SVG do arquivo, sanitizar (remover scripts), inserir no template como `<svg>` inline em vez de `<img src="...">`
    - [ ] Quando desativado: reverter para `<img src="assets/{nome}.svg">`
    - [ ] Criar helper `frontend/src/utils/svgSanitizer.ts` — remove `<script>`, `on*` attributes, `javascript:` URLs

- [ ] Task 7 — Implementar galeria de assets do projeto (AC: 6)
  - [ ] Criar `frontend/src/components/molecules/AssetGallery.vue`
    - [ ] Props: `templateId: string`, `onSelect: (asset: AssetInfo) => void`
    - [ ] Carregar lista via `assetService.listAssets(templateId)` ao montar
    - [ ] Grid de thumbnails com nome e tamanho ("logo.png — 24 KB")
    - [ ] Clicar em thumbnail emite `select` com asset selecionado
    - [ ] Estado vazio: "Nenhum asset no projeto"
  - [ ] Integrar como seção colapsável no ImageInspector: "Assets do projeto"

- [ ] Task 8 — Testes unitários (AC: 1-7)
  - [ ] `frontend/src/services/assetService.spec.ts`: upload, delete, list (mock HTTP)
  - [ ] `frontend/src/utils/imageValidation.spec.ts`: todos os cenários de validação (tipo, tamanho, dimensões)
  - [ ] `frontend/src/utils/svgSanitizer.spec.ts`: remoção de scripts e atributos perigosos
  - [ ] `frontend/src/components/molecules/ImageReplacePreview.spec.ts`: renderização, emits
  - [ ] `frontend/src/components/molecules/AssetGallery.spec.ts`: carregamento, seleção, estado vazio
  - [ ] `frontend/src/components/organisms/inspectors/ImageInspector.spec.ts`: fluxo substituir, remover, SVG toggle

- [ ] Task 9 — Build e verificação (AC: 7)
  - [ ] Executar `npm run build` e corrigir erros
  - [ ] Verificar integração no EditorLayout

### FRs Referenciados
- FR14: Extração e gerenciamento de imagens do PDF — substituição, download, remoção, dimensões/escala/alinhamento
- FR32: Detecção de imagens vetoriais (SVG) e incorporação como SVG inline

## Dev Notes
- **FR14**: imagens na pasta `assets/`, referenciadas como `<img src="assets/logo.png">`
- **FR32**: SVGs detectados oferecem opção de inline — sanitização obrigatória antes de inserir
- Substituição de imagem: file picker → validação → preview → upload → atualizar referência
- Remoção em cascata: nó (tree) + referência (HTML) + arquivo (servidor)
- Stack: Vue 3 + TypeScript + Pinia + Tailwind CSS
- Endpoints backend (a serem criados/confirmados com backend):
  - `POST /api/templates/{template_id}/assets` — upload multipart
  - `DELETE /api/templates/{template_id}/assets/{filename}` — remoção
  - `GET /api/templates/{template_id}/assets` — listagem

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação (split de 9.8 antiga) | River (@sm) |
| 2026-03-16 | 2.0 | Reescrita PO: 7 ACs granulares, 9 tasks detalhadas, boundary vs 6.7/7.4, FR32 SVG | River (@sm) |
