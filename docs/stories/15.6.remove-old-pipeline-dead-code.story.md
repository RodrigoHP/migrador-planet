---
id: "15.6"
title: "Remove old 28-stage pipeline + dead code audit"
type: refactor
status: InProgress
priority: high
epic: 15
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, regression_test]
---

# Story 15.6 — Remove Old 28-Stage Pipeline + Dead Code Audit

## Story

**Como** desenvolvedor do projeto,
**Quero** remover o pipeline antigo de 28 stages e código morto associado,
**Para** reduzir confusão, manter o codebase limpo, e evitar que futuros devs confundam pipeline v1 com v2.

## Status

InProgress

## Context

### Problema

O Epic 13 redesenhou o pipeline de 28 stages → 5 stages. Porém o código antigo ainda existe:
1. `models/pipeline.py` (202 lines): `StageRegistry`, `BlockDefinition`, `StageDefinition`, `build_default_registry()` com 8 blocks e 28 stages
2. `services/stages/register_all.py` (243 lines): `register_all()` que registra os 28 stages no `StageRegistry`
3. 31 helper files em `services/stages/` — alguns usados pelo pipeline v2 (5 stages), outros são dead code do v1
4. `main.py` (line 14): chama `register_all()` na startup

O pipeline v2 usa `pipeline_orchestrator_v2.py` que **não usa** o `StageRegistry`. Ele chama diretamente `stage1_layout_clustering.py` a `stage5_template_generation.py`.

### Estado Atual do Código

**`backend/models/pipeline.py`** (202 lines):
- Lines 1-40: `PipelineDefinition`, `StageDefinition`, `BlockDefinition` classes
- Lines 42-136: `StageRegistry` class (register, get, build, remove)
- Lines 138-196: `build_default_registry()` — 8 blocks, 28 stages
- Line 202: `default_registry = build_default_registry()` — module-level singleton

**`backend/services/stages/register_all.py`** (243 lines):
- 9 `register_blocoN()` functions que registram stages no `StageRegistry`
- `register_all()` master function (line 231)
- Called from `main.py` at startup

**`backend/services/pipeline_orchestrator_v2.py`** (397 lines):
- Pipeline v2 que chama stage1-5 diretamente
- **Não importa** `StageRegistry` nem `register_all`

### Scope — IN vs OUT

**IN SCOPE:**
- Remover `StageRegistry`, `BlockDefinition`, `build_default_registry()` de `models/pipeline.py`
- Remover `register_all.py` inteiro
- Remover chamada `register_all()` de `main.py`
- Auditar cada um dos 31 helper files em `stages/`:
  - Se importado por stage1-5: MANTER
  - Se não importado por nenhum stage ativo: REMOVER
- Corrigir imports quebrados
- Validar com grep que nenhum import referencia entidades removidas

**OUT OF SCOPE:**
- Refactor dos 5 stages ativos
- Mover `_deprecated/` directory
- Mudanças em testes dos 5 stages (devem passar sem modificação)

## Acceptance Criteria

- [ ] `StageRegistry` class removida de `models/pipeline.py` — BLOQUEADO: test_pipeline.py importa diretamente StageRegistry, BlockDefinition, build_default_registry. Remoção quebraria testes.
- [ ] `BlockDefinition` class removida — BLOQUEADO: mesma razão
- [ ] `StageDefinition` class — BLOQUEADO: mesma razão
- [ ] `build_default_registry()` removida — BLOQUEADO: 8 test files importam esta função
- [ ] `default_registry` singleton removido — BLOQUEADO: dependência de build_default_registry
- [ ] `register_all.py` removido de `services/stages/` — BLOQUEADO: 8 test files importam funções de register_all
- [x] `register_all()` call removida de `main.py` — JÁ FEITO no PR #23 (main.py sem nenhuma referência a register_all)
- [x] Cada helper file auditado com resultado documentado (ver Dev Notes abaixo)
- [ ] Dead helper files removidos — BLOQUEADO: todos os helpers são importados por testes existentes
- [x] `stages/__init__.py` verificado — não exporta nada removível (apenas docstring)
- [x] Zero imports quebrados: `from.*pipeline import.*StageRegistry` — main.py não importa
- [ ] Zero imports quebrados: `from.*register_all import` — 8 test files ainda importam
- [ ] Todos os testes existentes passam sem modificação — passam APENAS se register_all.py e helpers são mantidos
- [x] `PipelineDefinition` mantida — usada em test_pipeline.py

## Technical Notes

- **Auditoria de helpers:** Usar `grep -r "from.*stages.*import" backend/services/stages/stage[1-5]*.py` para identificar quais helpers são usados.
- **Cuidado:** Alguns helpers podem ser importados por testes, não por stages. Testes que importam helpers removidos precisam ser atualizados.
- **`models/pipeline.py`:** Pode manter `PipelineDefinition` se usada como Pydantic schema em responses.

## File List

| File | Action | Description |
|------|--------|-------------|
| `backend/models/pipeline.py` | EDIT | Remove StageRegistry, BlockDefinition, build_default_registry |
| `backend/services/stages/register_all.py` | DELETE | Entire file — old pipeline registration |
| `backend/main.py` | EDIT | Remove register_all() import and call |
| `backend/services/stages/__init__.py` | EDIT | Update exports if needed |
| `backend/services/stages/*.py` (dead helpers) | DELETE | Each dead file identified in audit |
| `backend/tests/*` | EDIT | Fix any broken imports from removed files |

## Dependencies

- Nenhuma — pode ser executada independente das outras waves

## Dev Notes

### Helper File Audit (2026-03-28)

Audit method: checked imports in active non-test, non-register_all backend code.

| Helper | Active Imports (non-test) | Decision |
|--------|--------------------------|---------|
| confidence_scoring | 0 | REMOVE — but blocked by test_matching.py |
| field_matching | 0 | REMOVE — but blocked by test_field_matching.py, test_matching.py |
| fingerprint_generation | 0 | REMOVE — but blocked by test_layout_discovery.py |
| font_extraction | 0 | REMOVE — but blocked by test_pdf_parsing.py |
| format_detection | 1 (stage4_field_mapping.py) | KEEP — used by active stage4 |
| grid_detection | 0 | REMOVE — but blocked by test_pdf_parsing.py |
| image_extraction | 0 | REMOVE — but blocked by test_storage_gateway_integration.py |
| intelligence_normalization | 0 | REMOVE — but blocked by test_layout_intelligence.py |
| layout_alignment | 0 | REMOVE — but blocked by test_layout_intelligence.py |
| layout_consistency | 0 | REMOVE — but blocked by test_validation_template.py |
| multi_example_analysis | 0 | REMOVE — but blocked by test_layout_intelligence.py |
| page_clustering | 0 | REMOVE — but blocked by test_layout_discovery.py |
| pipeline_result | 0 | REMOVE — but blocked by test_matching.py, test_pipeline_result.py |
| registry_lookup | 0 | REMOVE — but blocked by test_layout_discovery.py |
| representative_selection | 1 (fingerprint_generation.py) | KEEP — used by fingerprint_generation |
| screenshot_generator | 0 | REMOVE — but blocked by test_pdf_parsing.py |
| semantic_analysis | 0 | REMOVE — but blocked by test_table_semantic.py |
| skeleton_builder | 0 | REMOVE — but blocked by test_layout_discovery.py |
| stability_classification | 0 | REMOVE — but blocked by test_layout_intelligence.py |
| table_detection | 0 | REMOVE — but blocked by test_table_semantic.py |
| table_structuring | 0 | REMOVE — but blocked by test_table_semantic.py |
| template_draft | 0 | REMOVE — but blocked by test_template_draft.py, test_validation_template.py |
| text_extraction | 0 | REMOVE — but blocked by test_pdf_parsing.py, test_multipdf_pipeline.py |
| text_reconstruction | 0 | REMOVE — but blocked by test_pdf_parsing.py |
| variant_detection | 0 | REMOVE — but blocked by test_layout_intelligence.py |
| vision_self_check | 0 | REMOVE — but blocked by test_vision_ai.py (uses register_bloco6_vision) |
| visual_interpretation | 0 | REMOVE — but blocked by test_vision_ai.py |
| visual_segmentation | 1 (vision_self_check.py) | KEEP if vision_self_check kept |
| xsd_parser | 2 (stage4_field_mapping.py, _deprecated/jobs.py) | KEEP — used by active code |

**Conclusion:** To fully remove dead code, tests that directly test the old v1 pipeline helpers would need to be rewritten to test them indirectly through the v2 pipeline or removed. This is a larger scope than originally estimated (S → M or L). Recommend creating a follow-up story to migrate the tests.

## Estimation

S (Small) — Deletar código é rápido, audit de imports requer cuidado

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-03-28 | @dev (Dex) | Auditoria completa de 29 helper files. register_all() já removido de main.py (PR #23). Remoção de register_all.py e helpers bloqueada: 8 test files importam de register_all, todos os helpers importados por testes existentes. Documented audit in Dev Notes. Status → InProgress (blocked by test dependencies) |
