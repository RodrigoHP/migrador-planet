---
epic: 7
story: 5
title: "VisibilityControl"
status: Approved
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [static_analysis, ui_behavior_check]
depends_on: [7.4, 6.1]
---

# Story 7.5: VisibilityControl

## Status
Draft

## Story
**As a** operador,
**I want** definir a visibilidade de elementos como "Sempre visível", "Condicional" ou "Escondido" via construtor visual de condições (SE [campo] [operador] [valor] com E/OU),
**so that** o template gere blocos `<!-- ko if -->` apropriados.

## Acceptance Criteria
1. Dropdown de visibilidade no Inspector (todos os níveis exceto Page): "Sempre visível" (padrão), "Condicional", "Escondido" (FR9)
2. "Condicional" expande construtor de condições abaixo do dropdown
3. Construtor de condição: SE [campo dropdown] [operador dropdown] [valor input]
4. Operadores: existe, não existe, igual a, diferente de, maior que, menor que, contém
5. Condições compostas: botões [+ E] [+ OU] para adicionar condições
6. Preview do código Knockout gerado: `<!-- ko if: expressão -->`
7. "Escondido" remove elemento do HTML gerado inteiramente
8. Campos detectados como opcionais pela análise MultiDoc (pipeline) vêm pré-configurados como "Condicional"
9. Alterações de condição atualizam templateStore → Canvas re-renderiza mostrando/escondendo elementos
10. `npm run build` passa sem erros

## CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [ ] Implementar dropdown de visibilidade no Inspector (AC: #1)
  - [ ] Três opções: "Sempre visível", "Condicional", "Escondido"
  - [ ] Disponível em todos os níveis de inspeção exceto Page
- [ ] Implementar construtor visual de condições (AC: #2, #3, #4)
  - [ ] Componente VisibilityControl molecule: dropdown + construtor expansível
  - [ ] Campo dropdown: populado a partir da árvore de campos XSD (mappingStore.allFields)
  - [ ] Operador dropdown: existe, não existe, igual a, diferente de, maior que, menor que, contém
  - [ ] Input de valor: texto livre para comparações
- [ ] Implementar condições compostas (AC: #5)
  - [ ] Botão [+ E] adiciona condição com operador AND
  - [ ] Botão [+ OU] adiciona condição com operador OR
  - [ ] Lista visual de condições com indicadores E/OU
- [ ] Implementar preview do código Knockout (AC: #6)
  - [ ] Geração: "existe" → `field`, "não existe" → `!field`, "igual a" → `field() === value`, "maior que" → `field() > value`
  - [ ] Composto: E → `&&`, OU → `||`
  - [ ] Exibir preview formatado abaixo do construtor
- [ ] Implementar comportamento "Escondido" (AC: #7)
  - [ ] Remover elemento do HTML gerado (não apenas display:none)
- [ ] Implementar pré-configuração de condicionais do pipeline (AC: #8)
  - [ ] Ler multiDocStore.conditionalSections[]
  - [ ] Auto-popular visibilidade como "Condicional" para campos opcionais
- [ ] Implementar reatividade com Canvas (AC: #9)
  - [ ] Alterações de condição atualizam templateStore
  - [ ] Canvas re-renderiza mostrando/escondendo elementos conforme condição
- [ ] Verificar `npm run build` passa (AC: #10)

## Dev Notes
- FR9: "construtor visual SE [campo] [operador] [valor] com E/OU"
- Wireframe: Inspetor de Seção Opcional mostra construtor de condições
- Campo dropdown: populado de mappingStore.allFields (árvore de campos XSD)
- Geração Knockout: "existe" → `field`, "não existe" → `!field`, "igual a" → `field() === value`, "maior que" → `field() > value`
- Composto: E → `&&`, OU → `||`
- Exemplo: SE [telefone] [existe] OU [email] [existe] → `<!-- ko if: telefone || email -->`
- VisibilityControl molecule: dropdown + construtor de condições expansível
- Condicionais pré-configurados do pipeline: multiDocStore.conditionalSections[] → auto-popular visibilidade
- Stack: Vue 3 + TypeScript + Pinia + Tailwind CSS

### FRs Referenciados
- FR9: Condicionais — construtor visual SE [campo] [operador] [valor] com E/OU, gera `<!-- ko if -->`

### Testing
- Vitest for unit tests
- Test files alongside source: `*.spec.ts` or `*.test.ts`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
