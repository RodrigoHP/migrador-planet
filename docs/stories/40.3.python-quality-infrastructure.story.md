# Story 40.3 — Python Quality Infrastructure

## Status: InProgress

## Descricao

Eliminar vendored dependencies (32MB) e estabelecer tooling de qualidade para o backend Python: ruff (linting + formatting), mypy (type checking), e requirements.txt como unica fonte de dependencias. Resolve dois dos quatro debitos CRITICAL do sistema.

## Debitos Enderecados

| ID | Debito | Severidade | Esforco |
|----|--------|-----------|---------|
| SYS-001 | Vendored dependencies (32MB) em `backend/vendor/` -- patches de seguranca nao aplicados | CRITICAL | 4h |
| SYS-002 | Sem linter/formatter Python -- sem pyproject.toml, ruff.toml, mypy.ini | CRITICAL | 4h |

## Acceptance Criteria

- [x] `backend/vendor/` removido do repositorio
- [x] `conftest.py` nao adiciona vendor ao `sys.path`
- [x] Todas as dependencias instaladas via `pip install -r requirements.txt`
- [x] `pyproject.toml` configurado com ruff (linting + formatting) e mypy
- [x] `ruff check .` passa sem erros (apos `--fix` no bootstrap)
- [x] `mypy` configurado com strictness adequada (gradual typing)
- [ ] CI pipeline inclui `ruff check` e `mypy` como gates
- [ ] Documentacao de setup atualizada

## Escopo

### IN
- Remocao de `backend/vendor/` e referencias em `conftest.py`
- Verificacao de que `requirements.txt` cobre todas as deps vendoradas
- Criacao de `pyproject.toml` com configuracao ruff + mypy
- Execucao de `ruff --fix` para auto-correcoes
- Baseline de erros mypy (allowlist para correcao gradual)

### OUT
- Pre-commit hooks (Story 40.4, depende de SYS-003 tambem)
- ESLint/Prettier para frontend (Story 40.4)
- Correcao de todos os erros mypy (gradual, durante desenvolvimento)

## Estimativa

8h

## Dependencias

Nenhuma. Pode ser executada em paralelo com Story 40.1 e 40.2.

## Riscos

| Fix | Risco | Probabilidade | Mitigacao |
|-----|-------|--------------|-----------|
| SYS-001 (remove vendor) | Dependencia faltando no requirements.txt | MEDIA | Comparar imports em vendor/ com requirements.txt antes de remover |
| SYS-002 (ruff) | Centenas de warnings | BAIXA | `ruff --fix` no bootstrap + regras incrementais |

## Change Log

| Data | Agente | Mudanca |
|------|--------|---------|
| 2026-04-09 | @po (Pax) | validate: Story 40.3 PO gate 9.5/10 GO -- Draft -> Ready |
| 2026-04-09 | @dev (Dex) | implement: SYS-001 vendor/ removed (32MB), conftest.py cleaned, httpx+pydantic added to requirements.txt |
| 2026-04-09 | @dev (Dex) | implement: SYS-002 pyproject.toml with ruff+mypy, 1575 auto-fixes, 73 files formatted, 34 mypy baseline errors |

## File List

- `backend/conftest.py` — removed vendor sys.path manipulation
- `backend/requirements.txt` — added httpx, pydantic, ruff, mypy
- `backend/pyproject.toml` — NEW: ruff + mypy + pytest configuration
- `backend/vendor/` — REMOVED (32MB vendored dependencies)
- `backend/services/stages/stage1_layout_clustering.py` — ruff fixes (unused var, formatting)
- `backend/services/stages/stage2_deep_extraction.py` — ruff fixes (unused var, formatting)
- `backend/services/stages/stage3_structural_analysis.py` — ruff fixes (unused vars, formatting)
- `backend/tests/test_upload_unified.py` — added missing Path import
- `backend/tests/test_pipeline_e2e.py` — removed duplicate test function
- 73 files reformatted by ruff format

## Dev Notes

### SYS-001: Vendor Removal
- All 17 vendored packages are transitive dependencies of `openai` (pydantic, httpx, anyio, certifi, etc.)
- Only `pydantic` and `httpx` were directly imported by project code — added to requirements.txt explicitly
- No patched/modified files found in vendor/ — all were standard PyPI releases
- Tests pass after removal (131 verified, pre-existing benchmark/e2e failures unrelated)

### SYS-002: Ruff + Mypy Setup
- ruff rules: E, F, W, I (isort), UP (pyupgrade) — practical starter set
- ruff --fix auto-corrected 1575 issues (import ordering, syntax modernization, etc.)
- ruff format reformatted 73 files (double quotes, line length 120, LF endings)
- mypy baseline: 34 errors in 15 files (gradual mode, ignore_missing_imports=true)
- mypy segfaults on full scan with Python 3.14 — runs fine on specific directories
- Common mypy issues: no-any-return (9), unused-ignore (4), var-annotated (3), arg-type (4)
