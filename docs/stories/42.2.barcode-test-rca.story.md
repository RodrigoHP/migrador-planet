# Story 42.2 — RCA + Fix: 3 Falhas de Barcode Pré-existentes

## Status: Draft

## Descricao

Investigar e corrigir as 3 falhas de testes de barcode que existem desde antes dos Epics 40+41. Foram documentadas como "pré-existentes e não introduzidas" em múltiplas stories (41.3, 41.9), mas nunca investigadas.

## Falhas Conhecidas

| Teste | Arquivo | Sintoma |
|-------|---------|---------|
| `TestBarcodeFormatMap` | `backend/tests/` (a identificar) | Falha em mapeamento de formato |
| `TestBarcodeNodeRendering` | `backend/tests/` (a identificar) | Falha em renderização de nó |
| 3ª falha | a identificar | a identificar |

## Acceptance Criteria

- [ ] RCA completo: causa raiz identificada para cada uma das 3 falhas
- [ ] Fix implementado para cada falha (ou decisão documentada de aceitar como known limitation)
- [ ] Suite de testes backend passa com **zero falhas** (exceto as aceitas explicitamente)
- [ ] Falhas não reintroduzidas após Story 41.9 (barcode engine unification)

## Escopo

### IN
- Investigação das 3 falhas (`*investigate` / RCA)
- Fix nos arquivos de teste ou no código de produção
- Verificação de regressão após 41.9

### OUT
- Adição de novos testes de barcode além do fix
- Refatoração do módulo de barcode além do necessário

## Estimativa

5h

## Dependencias

- Story 41.9 (barcode engine unification) — done
- Story 41.13 (BarcodeInspector display) — done

## Dev Notes

### Contexto

Story 41.9 mudou o backend de `python-barcode` (SVG estático) para placeholder `<div data-type="barcode">`. As falhas pré-existentes podem estar relacionadas a:
- `_FORMAT_MAP` no stage5 que mapeava formatos barcode — pode ter sido alterado ou removido
- `BarcodeNodeRendering` que esperava SVG — pode precisar ser atualizado para o novo formato div
- Outros mocks desatualizados

### Abordagem RCA

Usar `/investigate` (FAST layer) — pattern matching esperado ser suficiente dado o contexto conhecido.

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-10 | @pm | Story criada — falhas pré-existentes documentadas em 41.3 e 41.9 |
