---
epic: 5
story: 3
title: "UploadPage v3 — Multi-PDF, XSD Obrigatório e Hints Contextuais"
status: Ready for Review
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [static_analysis, ui_behavior_check]
depends_on: [5.1]
---

# Story 5.3: UploadPage v3 — Multi-PDF, XSD Obrigatório e Hints Contextuais

## Status
Ready for Review

## Story
**As a** operador da ferramenta,
**I want** fazer upload de múltiplos PDFs (1 obrigatório, 3-5 recomendado), um XSD (obrigatório) e opcionalmente um arquivo de dados (XML/JSON), com hints contextuais me guiando,
**so that** eu possa fornecer todos os inputs necessários para o pipeline de análise.

## Acceptance Criteria
1. Três dropzones: PDFs (múltiplos, 1 obrigatório), XSD (single, obrigatório), Data (single, opcional XML/JSON)
2. Dropzone PDF mostra lista de arquivos com botão 🗑️ remover e contagem "📊 N PDFs (recomendado: 3-5)"
3. Campo de input para nome do template no topo
4. Hint quando sem arquivos: "Envie ao menos 1 PDF + XSD para continuar"
5. Hint quando 1 PDF apenas: "💡 Adicionar mais PDFs melhora a detecção de variações"
6. Hint quando PDF+XSD sem dados: "💡 Adicionar dados reais melhora a detecção de tipos e formatos"
7. Hint quando PDF+dados sem XSD: "💡 Adicionar o XSD permite validar campos obrigatórios"
8. Botão "Iniciar Análise" desabilitado até ao menos 1 PDF + XSD carregados
9. "Iniciar Análise" chama backend API POST /api/upload e navega para /analyzing
10. "← Voltar" retorna à Home
11. Upload rejeita PDFs maiores que 50MB por arquivo e XSD/Data maiores que 10MB, com mensagem de erro descritiva ("Arquivo excede o tamanho máximo de {limite}")
12. Progresso de upload exibido: barra de progresso por arquivo durante o envio ao backend (XMLHttpRequest.upload.onprogress ou fetch com ReadableStream)
13. `npm run build` passa

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [x] Task 1 — Reestruturar UploadPage com 3 dropzones (AC: 1)
  - [x] Dropzone PDFs: aceita `.pdf`, múltiplos arquivos, label "📄 PDFs do documento"
  - [x] Dropzone XSD: aceita `.xsd`, arquivo único, label "📋 Schema XSD"
  - [x] Dropzone Data: aceita `.xml, .json`, arquivo único, label "📊 Dados (opcional)"
  - [x] Cada dropzone com drag-and-drop e click-to-browse

- [x] Task 2 — Lista de PDFs com remoção e contagem (AC: 2)
  - [x] Exibir lista de PDFs carregados abaixo do dropzone
  - [x] Cada item com nome do arquivo e botão 🗑️ para remover
  - [x] Contagem: "📊 N PDFs (recomendado: 3-5)"
  - [x] Atualizar contagem em tempo real ao adicionar/remover

- [x] Task 3 — Campo nome do template (AC: 3)
  - [x] Input text no topo da página: "Nome do Template"
  - [x] Placeholder: "Ex: Extrato Bancário, Nota Fiscal..."
  - [x] Armazenar valor no estado local

- [x] Task 4 — Implementar hints contextuais (AC: 4, 5, 6, 7)
  - [x] Computed para estado dos arquivos: `hasPdf`, `hasXsd`, `hasData`, `pdfCount`
  - [x] Hint sem arquivos: quando `!hasPdf && !hasXsd` — "Envie ao menos 1 PDF + XSD para continuar"
  - [x] Hint 1 PDF: quando `pdfCount === 1 && hasXsd` — "💡 Adicionar mais PDFs melhora a detecção de variações"
  - [x] Hint sem dados: quando `hasPdf && hasXsd && !hasData` — "💡 Adicionar dados reais melhora a detecção de tipos e formatos"
  - [x] Hint sem XSD: quando `hasPdf && hasData && !hasXsd` — "💡 Adicionar o XSD permite validar campos obrigatórios"
  - [x] Estilo: hints informativos com fundo azul claro

- [x] Task 5 — Botão "Iniciar Análise" com validação (AC: 8, 9)
  - [x] Botão disabled quando `!hasPdf || !hasXsd`
  - [x] Ao clicar: construir FormData com PDFs, XSD, Data e nome do template
  - [x] Chamar POST /api/upload via XMLHttpRequest (para suportar onprogress)
  - [x] Armazenar job_id retornado no sessionStore
  - [x] Navegar para `/analyzing`

- [x] Task 6 — Botão "← Voltar" (AC: 10)
  - [x] Botão "← Voltar" navega para `/` (Home)
  - [x] Posicionado no topo da página

- [x] Task 7 — Implementar validação de tamanho de arquivo (AC: 11)
  - [x] PDF: máximo 50MB por arquivo (50 * 1024 * 1024 bytes)
  - [x] XSD/Data: máximo 10MB (10 * 1024 * 1024 bytes)
  - [x] Mensagem de erro descritiva ao rejeitar

- [x] Task 8 — Implementar progresso de upload (AC: 12)
  - [x] Barra de progresso durante envio via ProgressBar atom
  - [x] Usar XMLHttpRequest.upload.onprogress para rastrear progresso

- [x] Task 9 — Verificar build (AC: 13)
  - [x] Executar `npm run build` — PASS (0 erros)

## File List
- `frontend/src/pages/UploadPage.vue` — reescrito completamente

## Completion Notes
- Story implementada em 2026-03-17 por @dev (Dex / Claude Sonnet 4.6)
- FileDropzone existente suporta apenas File|null (single file); para multi-PDF foi implementado dropzone inline customizado com `input[multiple]` + drag-and-drop nativo no pai
- sessionStore.jobId setado antes de router.push('/analyzing') — router guard validado
- FormData construído com `pdfs[]` (array), `xsd`, `data`, `template_name` — multipart correto
- XMLHttpRequest usado (não fetch) para ter acesso a upload.onprogress
- Build passou sem warnings críticos (apenas 2 empty chunks de pdfjs/chartjs existentes antes desta story)

## Dev Notes

### Wireframe Referência
- Tela 1 — Upload: 3 dropzones (PDFs, XSD, Data)
- PDFs aceita múltiplos arquivos, os outros são single

### Formatos Aceitos
- PDF: `.pdf` (múltiplos)
- XSD: `.xsd` (único)
- Data: `.xml` ou `.json` (único, opcional)

### Backend Endpoint
- POST /api/upload com multipart form data
- Campos: `pdfs[]` (multiple files), `xsd` (single file), `data` (single file), `template_name` (string)
- Retorna: `{ job_id: string }`

### Estado dos Arquivos
- Armazenar arquivos em refs locais antes de enviar
- Não enviar para backend até clicar "Iniciar Análise"
- sessionStore armazena job_id após upload bem-sucedido

### Layout
- Usar `FullWidthLayout.vue` como layout da página

### FRs Referenciados
- FR1: Upload de PDF(s) para análise
- FR2: Upload de XSD para definição de campos
- FR2a: Upload opcional de dados (XML/JSON) para melhorar detecção

### Stack
- Vue 3 + TypeScript + Tailwind CSS 4.x
- XMLHttpRequest para chamada ao backend (requerido para onprogress)

### Testing
- Vitest for unit tests
- Test files alongside source: `*.spec.ts` or `*.test.ts`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
| 2026-03-16 | 1.1 | Ajustes PO: depends_on, AC limite upload, AC progresso upload | River (@sm) |
| 2026-03-17 | 2.0 | Implementação completa — todas tasks concluídas, build PASS | Dex (@dev) |
