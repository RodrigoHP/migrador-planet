---
id: "15.2"
title: "Deploy frontend Vercel + API URL config"
type: feat
status: Ready
priority: critical
epic: 15
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [infra_review, build_validation]
---

# Story 15.2 — Deploy Frontend Vercel

## Story

**Como** equipe de desenvolvimento,
**Quero** o frontend Vue 3 rodando no Vercel com API apontando para o Railway,
**Para** que a aplicação seja acessível publicamente via URL.

## Status

Done

## Context

### Problema

O frontend roda em `localhost:5173` e usa Vite proxy para `localhost:8000`. Em produção, precisa apontar para a URL real do Railway. Não existe `vercel.json`. SPA routing precisa de fallback para `index.html`.

### Estado Atual do Código

**`frontend/vite.config.ts`** — proxy configurado para dev:
```typescript
server: {
  proxy: {
    '/api': 'http://localhost:8000'
  }
}
```

**`frontend/src/router/index.ts`** — 4 rotas com lazy loading:
- `/` → HomePage
- `/upload` → UploadPage
- `/analyzing` → AnalyzingPage (guard: requires jobId)
- `/editor` → TemplateEditor (guard: requires analysisCompleted)

**API calls** — usam paths relativos (`/api/...`), passando pelo Vite proxy em dev.

### Scope — IN vs OUT

**IN SCOPE:**
- Criar `vercel.json` com SPA rewrites + API rewrites para Railway
- Atualizar `.env.example` com `VITE_API_URL` como referência documental (não como mecanismo ativo de roteamento)
- Configurar Vercel project + env vars
- Validar preview URLs em PRs

**OUT OF SCOPE:**
- Custom domain
- Autenticação (story 15.3)
- SSR/ISR (app é SPA pura)

### Decisão Técnica: API Routing

**Opção recomendada: Vercel Rewrites** — manter API calls relativos (`/api/...`) e usar Vercel rewrites para proxy ao Railway. Zero mudança no código fonte.

```json
{
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://RAILWAY_URL/api/:path*" }
  ]
}
```

## Acceptance Criteria

- [x] `frontend/vercel.json` criado com SPA fallback + API rewrites
- [x] Vercel rewrite em `vercel.json` mapeia `/api/:path*` ao backend Railway (sem `VITE_API_URL` como mecanismo de roteamento)
- [x] `npm run build` passa sem erros no Vercel
- [x] Rota `/` (HomePage) carrega corretamente
- [x] Rota `/upload` (UploadPage) carrega e mostra dropzone
- [x] Rota `/analyzing` redireciona para `/upload` se sem jobId (guard funciona)
- [x] Rota `/editor` redireciona para `/` se sem analysis (guard funciona)
- [x] API calls (`/api/health`) chegam ao Railway via rewrite
- [x] Preview URLs geradas automaticamente em PRs
- [x] Testes existentes passam sem modificação

## Technical Notes

- Vercel detecta Vue 3 + Vite automaticamente (`framework: vite`)
- Rewrites são preferíveis a env var para evitar CORS issues (same-origin)
- SSE funciona via Vercel rewrites — verificar timeout de connections long-lived (Vercel suporta SSE em rewrites)

## File List

| File | Action | Description |
|------|--------|-------------|
| `frontend/vercel.json` | CREATE | SPA fallback + API rewrites |
| `.env.example` | EDIT | Add VITE_API_URL (referência) |

## Dependencies

- **15.1** — Backend Railway precisa estar live para configurar rewrite URL

## Estimation

XS (Extra Small) — 1 arquivo + config Vercel dashboard

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-03-28 | @dev (Dex) | Verificação: todos os ACs implementados no PR #23. Gap: .env.example sem VITE_API_URL (referência). Corrigido. Status → Done |
