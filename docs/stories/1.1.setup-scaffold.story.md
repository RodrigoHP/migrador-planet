---
epic: 1
story: 1
title: "Setup & Scaffold"
status: ReadyForReview
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [architecture_review, pattern_validation, bundle_size_check, typescript_check]
---

# Story 1.1: Setup & Scaffold

## Status

ReadyForReview

## Story

**As a** dev agent,
**I want** um scaffold completo do projeto frontend com estrutura Atomic Design, Vite configurado, Tailwind com tokens customizados, Vue Router com guards, 4 Pinia stores tipados e 4 composables base,
**so that** todas as stories subsequentes possam ser implementadas sobre uma base sólida, consistente e sem ambiguidade arquitetural.

## Acceptance Criteria

1. Projeto Vite + Vue 3 + TypeScript criado em `frontend/` com `npm run dev` funcional na porta 5173
2. Tailwind CSS configurado com paleta customizada completa (tokens de `docs/front-end-spec.md` seção 6.1)
3. Vue Router configurado com 6 rotas (`/`, `/upload`, `/campos`, `/layout`, `/geracao`, `/exportar`) e guards de navegação implementados
4. 4 Pinia stores criados com interfaces TypeScript exatas conforme `docs/front-end-spec.md` seção 9: `useSessionStore`, `useMappingStore`, `useLayoutStore`, `useGenerationStore`
5. 4 composables criados com assinaturas TypeScript: `useSSE`, `useFileSystem`, `useProject`, `useBibliotecas`
6. Estrutura de pastas Atomic Design criada: `atoms/`, `molecules/`, `organisms/`, `templates/`, `pages/`
7. `AppHeader` organism criado com logo, wizard step indicator placeholder e botão "Bibliotecas" placeholder
8. `AppToast` organism criado com suporte a variantes success/warning/error/neutral e auto-dismiss 4s
9. `vite.config.ts` configurado com proxy `/api` → `http://localhost:8000` para dev e manual chunks para Monaco, PDF.js e Chart.js
10. `npm run build` produz `frontend/dist/` sem erros TypeScript

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Task 1 — Criar projeto Vite (AC: 1)
  - [x] `npm create vite@latest frontend -- --template vue-ts`
  - [x] Instalar dependências: `pinia vue-router @vueuse/core idb lucide-vue-next`
  - [x] Instalar dependências pesadas: `pdfjs-dist monaco-editor chart.js`
  - [x] Instalar devDependencies: `tailwindcss @tailwindcss/forms autoprefixer`
  - [x] Verificar `npm run dev` funciona na porta 5173

- [x] Task 2 — Configurar Tailwind (AC: 2)
  - [x] Inicializar Tailwind: `npx tailwindcss init -p`
  - [x] Adicionar paleta customizada em `tailwind.config.ts` com tokens de `docs/front-end-spec.md` seção 6.1
  - [x] Configurar `@tailwindcss/forms` plugin
  - [x] Criar `src/assets/main.css` com `@tailwind base/components/utilities`

- [x] Task 3 — Configurar Vue Router (AC: 3)
  - [x] Criar `src/router/index.ts` com 6 rotas
  - [x] Implementar guards de navegação: `/campos` requer `session.extraction`, `/layout` requer `mapping.confirmed`, `/geracao` requer `layout.confirmed`, `/exportar` requer `generation.html`
  - [x] Registrar router no `main.ts`

- [x] Task 4 — Criar Pinia stores (AC: 4)
  - [x] `src/stores/session.ts` — `useSessionStore` com interface `SessionStore` completa
  - [x] `src/stores/mapping.ts` — `useMappingStore` com interface `MappingStore` e `FieldMapping`
  - [x] `src/stores/layout.ts` — `useLayoutStore` com interface `LayoutStore`
  - [x] `src/stores/generation.ts` — `useGenerationStore` com interfaces `GenerationStore`, `IASuggestion`, `ChartjsConfig`
  - [x] Registrar Pinia no `main.ts`

- [x] Task 5 — Criar composables (AC: 5)
  - [x] `src/composables/useSSE.ts` — assinatura: `function useSSE(jobId: Ref<string | null>)`
  - [x] `src/composables/useFileSystem.ts` — expõe `pickPDF`, `pickXSD`, `pickData`
  - [x] `src/composables/useProject.ts` — expõe `save`, `load`, `currentProjectName`
  - [x] `src/composables/useBibliotecas.ts` — expõe `catalog`, `isLoading`, `updateVersion`

- [x] Task 6 — Criar estrutura Atomic Design (AC: 6)
  - [x] Criar pastas: `src/atoms/`, `src/molecules/`, `src/organisms/`, `src/templates/`, `src/pages/`
  - [x] Criar arquivos index.ts em cada pasta para re-exports

- [x] Task 7 — Criar AppHeader e AppToast (AC: 7, 8)
  - [x] `src/organisms/AppHeader.vue` — logo, step indicator slot, botão "Bibliotecas" (emit)
  - [x] `src/organisms/AppToast.vue` — props: `variant`, `message`, auto-dismiss com `setTimeout` 4000ms

- [x] Task 8 — Configurar vite.config.ts (AC: 9)
  - [x] Proxy `/api` → `http://localhost:8000` (server.proxy)
  - [x] Manual chunks: `{ 'monaco': ['monaco-editor'], 'pdfjs': ['pdfjs-dist'], 'chartjs': ['chart.js'] }`
  - [x] `resolve.alias` para `@` → `./src`

- [x] Task 9 — Verificar build (AC: 10)
  - [x] `npm run build` sem erros TypeScript
  - [x] Verificar que `frontend/dist/` é gerado
  - [x] Verificar chunk sizes (Monaco, PDF.js, Chart.js fora do chunk principal)

## Dev Notes

### Stack Frontend
[Source: docs/front-end-spec.md#1-introducao]
- Vue 3 + TypeScript + Vite
- Pinia 2.x + Vue Router 4.x
- Tailwind CSS (base unit 4px)
- lucide-vue-next (ícones)
- Inter (fonte UI), JetBrains Mono (código)

### Estrutura de Pastas
[Source: docs/front-end-spec.md#12-next-steps]
```
frontend/src/
  atoms/          # Button, Badge, ProgressBar, Spinner, Icon, Input, Select
  molecules/      # FileDropzone, FieldStatusBadge, CrossValidationBadge...
  organisms/      # WizardStepper, PDFViewer, FieldMappingTable, MonacoTabs...
  templates/      # WizardLayout, SplitPaneLayout, FullWidthLayout
  pages/          # HomePage, UploadPage, CamposPage, LayoutPage, GeracaoPage, ExportarPage
  stores/         # session.ts, mapping.ts, layout.ts, generation.ts
  composables/    # useSSE.ts, useFileSystem.ts, useProject.ts, useBibliotecas.ts
  router/         # index.ts com guards de navegação
  types/          # interfaces exportadas
  assets/         # main.css, fontes
```

### Paleta de Cores Tailwind
[Source: docs/front-end-spec.md#6-branding-e-style-guide]
```typescript
// tailwind.config.ts — extend.colors
primary: { 600: '#2563EB', 700: '#1D4ED8' }
success: { 600: '#16A34A' }
warning: { 500: '#EAB308' }
error: { 600: '#DC2626' }
neutral: { 50: '#FAFAFA', 100: '#F5F5F5', 200: '#E5E5E5', 500: '#737373', 700: '#404040', 800: '#262626', 900: '#171717' }
```

### Interfaces TypeScript dos Stores
[Source: docs/front-end-spec.md#9-stores-pinia-e-composables]
Ver seção 9.1-9.4 do front-end-spec.md para interfaces completas de `SessionStore`, `FieldMapping`, `MappingStore`, `LayoutStore`, `GenerationStore`.

### Guards de Navegação
[Source: docs/front-end-spec.md#2-arquitetura-de-informacao]
```typescript
// Regras de guard (em router/index.ts):
'/campos'   → requer: sessionStore.extraction !== null
'/layout'   → requer: mappingStore.confirmed === true
'/geracao'  → requer: layoutStore.confirmed === true
'/exportar' → requer: generationStore.html !== null
```

### Vite Config — Manual Chunks
[Source: docs/front-end-spec.md#11-performance]
Bundle target: < 400KB gzip chunk inicial. Monaco, PDF.js e Chart.js devem ficar em chunks separados.

### Testing
Sem framework de testes configurado nesta story — foco em build sem erros TypeScript e estrutura correta.
`tsc --noEmit` deve passar sem erros.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Story criada | @sm (River) |
| 2026-03-11 | 1.1 | Implementacao do scaffold frontend (router, stores, composables, Tailwind e validacoes de qualidade) | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
GPT-5 Codex (Codex CLI)

### Completion Notes
- Retomada feita a partir de `frontend/resumodev.txt`; arquivo continha historico de validacao e nao checkpoint de implementacao.
- Bootstrap atualizado para usar `Pinia` + `Vue Router` no `main.ts`, com shell base no `App.vue`.
- Criados placeholders das 6 paginas para suportar as rotas definidas na story.
- Tipagem TypeScript corrigida com alias `@/*` no `tsconfig.app.json` e `src/env.d.ts`.
- Tailwind consolidado com tokens em `src/assets/main.css`, config em `tailwind.config.ts` e plugin forms.
- Build de producao validado com `frontend/dist/` gerado.
- Quality gates executados em `frontend`: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.
- Validacao de porta: `npm run dev -- --port 5173 --strictPort` retornou porta em uso e `netstat` confirmou listener em `127.0.0.1:5173`.

### File List
- frontend/package.json
- frontend/postcss.config.js
- frontend/tailwind.config.ts
- frontend/tsconfig.app.json
- frontend/src/env.d.ts
- frontend/src/main.ts
- frontend/src/App.vue
- frontend/src/assets/main.css
- frontend/src/router/index.ts
- frontend/src/stores/session.ts
- frontend/src/stores/mapping.ts
- frontend/src/stores/layout.ts
- frontend/src/stores/generation.ts
- frontend/src/composables/useSSE.ts
- frontend/src/composables/useFileSystem.ts
- frontend/src/composables/useProject.ts
- frontend/src/composables/useBibliotecas.ts
- frontend/src/organisms/AppHeader.vue
- frontend/src/organisms/AppToast.vue
- frontend/src/organisms/index.ts
- frontend/src/pages/index.ts
- frontend/src/pages/HomePage.vue
- frontend/src/pages/UploadPage.vue
- frontend/src/pages/CamposPage.vue
- frontend/src/pages/LayoutPage.vue
- frontend/src/pages/GeracaoPage.vue
- frontend/src/pages/ExportarPage.vue

## QA Results
### Review Date
2026-03-11

### Reviewer
@qa (Quinn)

### Gate Decision
PASS

### Evidence Executed
- `npm run lint` (frontend): PASS
- `npm run typecheck` (frontend): PASS
- `npm test` (frontend): PASS (script informativo, sem suíte automatizada)
- `npm run build` (frontend): PASS, `frontend/dist/` gerado

### Acceptance Criteria Validation
- AC1 a AC10: atendidos no escopo da story 1.1 e consistentes com o código atual.

### Findings
- Nenhum bug/regressão bloqueante identificado nesta revisão.

### Residual Risks / Gaps
- Não há suíte de testes automatizados de comportamento (unit/component/e2e) nesta story; o gate atual depende de tipagem e build.
- Chunking de bibliotecas pesadas (Monaco/PDF.js/Chart.js) está configurado, mas a validação efetiva desses chunks em runtime depende de uso real nas stories seguintes.
