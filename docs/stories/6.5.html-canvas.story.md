---
epic: 6
story: 5
title: "HTMLCanvas — Preview WYSIWYG do Template com Zoom e Guias Visuais"
status: Draft
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [static_analysis, ui_behavior_check]
depends_on: [6.1, 6.2]
---

# Story 6.5: HTMLCanvas — Preview WYSIWYG do Template com Zoom e Guias Visuais

## Status
Draft

## Story
**As a** operador,
**I want** ver o template HTML renderizado ao vivo em um iframe como preview WYSIWYG com controles de zoom e guias visuais,
**so that** eu possa ver exatamente como o template final ficará.

## Acceptance Criteria
1. Organism HTMLCanvas renderiza um iframe na aba "Canvas" do painel central
2. Iframe carrega o HTML+CSS do template draft de `generationStore.templateDraft`
3. Scroll contínuo vertical com páginas empilhadas (gap e sombra entre páginas)
4. Dimensões de página: A4 default (794×1123px), configurável a partir de templateStore
5. Controles de zoom no rodapé: [- 100% +] range 50%-125%
6. Guias visuais: margens da página (linhas pontilhadas), limites Header/Flow/Footer (linhas coloridas), colunas detectadas (guias verticais)
7. Guias ativáveis via `editorStore.showGuides`
8. Linhas de quebra de página visíveis entre páginas: "--- QUEBRA DE PÁGINA ---"
9. Lazy rendering: máximo 5 páginas visíveis, páginas adicionais carregam no scroll
10. CSS isolado no iframe (CSS do template não vaza para CSS do editor)
11. `npm run build` passa

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation via manual review + QA gate.

## Tasks / Subtasks
- [ ] Task 1 — Criar composable useCanvas (AC: 5, 9)
  - [ ] Criar `frontend/src/composables/useCanvas.ts`
  - [ ] Estado reativo: `zoomLevel` (ref, sync com editorStore.zoomLevel), `scrollPosition`, `visiblePages` (Set<number>)
  - [ ] Funções: `zoomIn()`, `zoomOut()`, `setZoom(level)`, `scrollToPage(pageNum)`
  - [ ] Lógica de lazy rendering: IntersectionObserver para detectar páginas no viewport
  - [ ] Limites de zoom: min 50%, max 125%, step 10%

- [ ] Task 2 — Criar organism HTMLCanvas (AC: 1, 2, 3)
  - [ ] Criar `frontend/src/components/organisms/HTMLCanvas.vue`
  - [ ] Container com overflow-y: auto para scroll contínuo
  - [ ] Renderizar iframe usando `srcdoc` ou blob URL com HTML+CSS de `generationStore.templateDraft`
  - [ ] Páginas empilhadas verticalmente: cada `<div class="page">` do template como uma página visual
  - [ ] Gap entre páginas (ex.: 24px) com sombra (box-shadow) em cada página
  - [ ] Background cinza entre páginas para efeito de "folhas de papel"

- [ ] Task 3 — Implementar isolamento de CSS no iframe (AC: 10)
  - [ ] Construir documento HTML completo para o iframe: `<!DOCTYPE html><html><head><style>${css}</style></head><body>${html}</body></html>`
  - [ ] Usar `srcdoc` para carregar no iframe
  - [ ] Verificar que estilos do template não afetam o editor
  - [ ] Verificar que estilos do editor (Tailwind) não afetam o template

- [ ] Task 4 — Implementar dimensões de página A4 (AC: 4)
  - [ ] Dimensões default A4: 794×1123px (210mm×297mm a 96dpi)
  - [ ] Cada página wrapper com dimensão fixa
  - [ ] Configurável via `templateStore.pageSettings.size`
  - [ ] Suportar: A4 (794×1123), Letter (816×1056), Custom

- [ ] Task 5 — Implementar controles de zoom (AC: 5)
  - [ ] Criar `ZoomControls.vue` (molecule) posicionado no rodapé do canvas
  - [ ] Botões: [-] [valor%] [+]
  - [ ] Conectar com useCanvas composable
  - [ ] CSS transform: `scale(zoomLevel / 100)` aplicado ao container das páginas
  - [ ] `transform-origin: top center` para zoom centralizado

- [ ] Task 6 — Implementar guias visuais (AC: 6, 7)
  - [ ] Criar `CanvasGuides.vue` (molecule) — overlay absolutamente posicionado sobre o iframe
  - [ ] Guia de margens: linhas pontilhadas nos 4 lados da página (azul claro, 1px dashed)
  - [ ] Guia Header/Flow/Footer: linhas horizontais coloridas (Header=azul, Flow=verde, Footer=laranja)
  - [ ] Guia de colunas: linhas verticais (cinza, 1px dashed) nas posições detectadas
  - [ ] Visibilidade controlada por `editorStore.showGuides`
  - [ ] Posições das guias derivadas de `templateStore` (margens, alturas de seção, posições de coluna)

- [ ] Task 7 — Implementar quebras de página (AC: 8)
  - [ ] Entre cada página, renderizar divisor visual: "--- QUEBRA DE PÁGINA ---"
  - [ ] Estilo: texto centralizado, cor cinza, fonte pequena, linhas tracejadas em cada lado

- [ ] Task 8 — Implementar lazy rendering (AC: 9)
  - [ ] Usar IntersectionObserver no useCanvas composable
  - [ ] Cada página tem um placeholder com altura fixa quando não visível
  - [ ] Máximo de 5 páginas renderizadas simultaneamente
  - [ ] Páginas fora do viewport são substituídas por placeholder
  - [ ] Manter scroll position estável ao trocar páginas

- [ ] Task 9 — Integrar no CenterPanel (AC: 1)
  - [ ] Inserir HTMLCanvas no slot da aba "Canvas" do CenterPanel (criado na 6.2)
  - [ ] Canvas ocupa 100% do espaço disponível

- [ ] Task 10 — Build e verificação (AC: 11)
  - [ ] Executar `npm run build` e corrigir erros
  - [ ] Criar teste unitário `HTMLCanvas.spec.ts`: renderiza com template mock, zoom controls, guias

## Dev Notes

### Canvas Renderiza o HTML REAL do Template
- O Canvas mostra o HTML gerado pelo pipeline — NÃO é uma imagem do PDF original
- Wireframe: scroll contínuo vertical, páginas empilhadas com gap
- O iframe é fundamental para isolamento de CSS

### Estratégia de Iframe
```typescript
// Construir srcdoc completo
const srcdoc = computed(() => {
  const { html, css } = generationStore.templateDraft
  return `<!DOCTYPE html>
<html>
<head><style>${css}</style></head>
<body>${html}</body>
</html>`
})
```

### Estrutura HTML do Template Draft
O pipeline gera HTML com a seguinte estrutura:
```html
<div class="page" data-page="1">
  <div class="header">...</div>
  <div class="flow">...</div>
  <div class="footer">...</div>
</div>
<div class="page" data-page="2">
  ...
</div>
```

### Zoom com CSS Transform
```css
.canvas-content {
  transform: scale(var(--zoom));
  transform-origin: top center;
}
```
O container externo mantém scroll, o conteúdo interno é escalado.

### Lazy Rendering com IntersectionObserver
```typescript
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    const pageNum = Number(entry.target.dataset.page)
    if (entry.isIntersecting) visiblePages.add(pageNum)
    else visiblePages.delete(pageNum)
  })
}, { rootMargin: '200px' }) // preload margin
```

### Interação (NÃO nesta story)
- Click/drag/resize de elementos no Canvas vem no Epic 7
- Por enquanto: display only, sem interação além de scroll e zoom

### FRs Referenciados
- FR7: Preview WYSIWYG do template

### Testing
- Vitest for unit tests
- Test files alongside source: `*.spec.ts` or `*.test.ts`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Criação | River (@sm) |
