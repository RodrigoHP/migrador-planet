# Story 42.6 — Stage 4 + Stage 5 Pydantic Migration

## Status: Draft

## Descricao

Migrar os sub-pacotes dos stages 4 e 5 de `dict[str, Any]` para modelos Pydantic tipados. O `result_assembly.py` (stage5) é o ponto crítico — ele constrói o JSON exato que o frontend recebe via API. Nenhum campo pode ser renomeado ou removido sem coordenação com story 42.8.

## Debitos Enderecados

| ID | Debito | Severidade | Esforco |
|----|--------|-----------|---------|
| 40.7-AC2 | Stage 4+5 usam dict[str,Any] — stage5 é o contrato direto com API | HIGH | 14h |

## Acceptance Criteria

- [ ] AC1: `section_matching.py` — 43 acessos a dict substituídos por atributos tipados (maior arquivo)
- [ ] AC2: `scoring_validation.py` — idem (20 occurrences)
- [ ] AC3: `xsd_integration.py` — idem (12 occurrences)
- [ ] AC4: `coverage_overlay.py` — idem (27 occurrences)
- [ ] AC5: `html_tree.py`, `html_helpers.py`, `css_generation.py`, `variation_matrix.py` — idem
- [ ] AC6: `result_assembly.py` — `_step_5_6_pipeline_result()` produz campos **idênticos** ao formato atual (zero breaking changes no JSON da API)
- [ ] AC7: `FieldMappingEntry` backend alinhado com interface frontend `FieldMappingEntry` em `pipeline.types.ts`
- [ ] AC8: mypy passa nos 9 arquivos migrados (zero erros novos)
- [ ] AC9: Todos os testes existentes passando (zero regressões)
- [ ] AC10: E2E tests passando com PDF de referência

## Escopo

### IN
- `backend/services/stages/stage4_mapping/` — 3 arquivos (~1.091 LOC, ~75 occurrences)
- `backend/services/stages/stage5_template/` — 6 arquivos (~1.953 LOC, ~84 occurrences)
- `backend/models/pipeline_context.py` — finalizar `FieldMappingEntry`, `PipelineResult`

### OUT
- `extra="allow"` → `extra="forbid"` — story 42.7
- Declarar `response_model=` no router — story 42.7
- Alterações nos tipos frontend — story 42.8

## Estimativa

14h

## Dependencias

- Story 42.5 (Stage 3 migrado) — stage4 consome outputs tipados do stage3
- Story 42.3 (Canvas SVG Sync) — não interfere, mas fecha o gap paralelo

## Riscos

| Risco | Probabilidade | Mitigacao |
|-------|--------------|-----------|
| `result_assembly.py` muda shape do JSON → quebra frontend | ALTA | Snapshot test do JSON antes/depois; AC6 obrigatório |
| `section_matching.py` (43 occurrences) — maior arquivo, alto risco | ALTA | Migrar em sub-tarefas: 1 função por vez + teste entre cada |
| `FieldMappingEntry` backend vs frontend drift | MEDIA | Comparar explicitamente com `pipeline.types.ts` antes de commitar |

## Dev Notes

### Prioridade de migração (ordem de execução)

1. **`xsd_integration.py`** (12 occurrences) — menor, menos crítico, bom warmup
2. **`scoring_validation.py`** (20 occurrences) — intermediate
3. **`section_matching.py`** (43 occurrences) — maior, migrar 1 função por vez
4. **`css_generation.py`**, **`variation_matrix.py`** — stage5 menores
5. **`html_helpers.py`**, **`html_tree.py`** — stage5 rendering
6. **`coverage_overlay.py`** (27 occurrences) — penúltimo
7. **`result_assembly.py`** — ÚLTIMO, mais crítico

### result_assembly.py — campos protegidos

Os campos abaixo em `_step_5_6_pipeline_result()` não podem mudar de nome sem coordenação com story 42.8:

```python
# Campos que o frontend lê diretamente (pipeline.types.ts:135-156)
"layout_types"          # list[LayoutType]
"field_mappings"        # list[FieldMappingEntry]
"trees_by_layout"       # dict[str, DocumentTree]
"document_structure"    # DocumentTree
"template_draft"        # { html, css }
"confidence_scores"     # dict[str, ConfidenceFactors]
"document_type"         # str
"document_type_confidence"  # float
```

### FieldMappingEntry — drift identificado pelo arquiteto

```typescript
// Frontend atual (pipeline.types.ts) — deve alinhar com backend após 42.6
interface FieldMappingEntry {
  field_id: string
  xsd_path: string
  confidence: number
  mapping_type: string
  // ...
}
```

Verificar `pipeline_context.py:FieldMappingEntry` e garantir campos idênticos.

## Change Log

| Date | Agent | Action |
|------|-------|--------|
| 2026-04-10 | @architect | Story criada — avaliação pós-audit Epic 40+41 |
